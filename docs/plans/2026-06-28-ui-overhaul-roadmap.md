# MFS-Operations — UI Overhaul Roadmap (single source of truth)

> **This is the MASTER UI tracker — the one place to track the whole UI-system rebuild.** Status,
> decisions, the design handover, Phase 0, and every screen in Phase 1+. Update the **Change log** at the
> bottom each session.
> **Tracker map (Hakan, 2026-06-30):** ① THIS = the master UI tracker · ② `wc29`
> (`docs/plans/2026-06-29-wc29-plan.md`) = the weekly execution log · label-printing detail lives ONCE in
> **BACKLOG §F-PROD-04** (trackers point to it, never duplicate). Reference docs (not session trackers):
> `docs/ui-current-state.md` (inventory), `docs/design/2026-06-27-ui-system-brand-prompt.md` (Claude Design
> brief), `docs/plans/2026-06-27-ui-system-rebuild-process.md` (per-section process contract).

**Status as of 2026-06-29 (Terminal-1):** ✅ Design delivered + reviewed (STRONG). ✅ **FORGE Phase 0a SHIPPED to `main`** — PR #93 squash `6a847fa`. ✅ **FORGE Phase 0b COMPLETE** — all 3 waves shipped: W1 Forms (PR #94 `ff7a005`) · W2 Display (PR #95 `9b91885`) · W3 Overlays+Nav (PR #96 `59a8f13`). **Phase 0 (0a+0b) is now effectively complete** (0c deferred/build-on-demand). **Per decision #16 the UI is PAUSED for label printing F-PROD-04** (repo/docs cleanup done; PR #97). **F-PROD-04 Pass 1 (PR #98) + 2a (Printer port, PR #99) + 2b (release-signed APK + MFS launcher icon, PR #100 `40f446d`, 2026-06-30) ✅ ALL SHIPPED.** die-cut label sizing (DELIVERY) ✅ SHIPPED 2026-06-30 (PR #101 `84e886a` + calibration `816455c`; 52×38mm label mode, version-tolerant JSON bridge ADR-0013, confirmed fitting on the V3). beef mince + meat-prep BLS DISPATCH labels ✅ FULLY DONE 2026-06-30 — BOTH labels confirmed printing + fitting on the V3 (PR #102 `0a3a1cb` + native calibration vc7/1.6 `d2d0ce6`; RPA-email-driven; MINCE country-only vs PREP country+plant + `Further cut in GB2946`; `MFS_PLANT_CODE` const; server-aggregates-once + native `format=json`; bridge `mince`/`prep` branches no-signature-change ADR-0013). **THREE on-device follow-on web fixes** (server-side, no APK reinstall): mince 404 (PR #103 `5ed1c24` — `allergens_present` selected from the wrong table → PostgREST 404); traceability chaining (PR #104 `c78d5eb` — `resolveDeliveryIds()` follows a prep/mince run's source BATCHES back to the underlying deliveries, so a burger built from a mince batch inherits Born/Slaughtered/Cut); Born/Reared as country CODES (GB,AU) for fit. ANVIL CLEARED unit 3016 · @critical 78/78 throughout. **Pass 3 (real allergens on the DELIVERY label) ✅ SHIPPED 2026-06-30 (PR #105 `13980c5`)** — web-only, no migration/native; `formatDeliveryAllergens` helper + `escapeHtml()` XSS hardening; ANVIL unit 3036 · integration 9/9 · prod smoke 5/5 → 200. **BEEF-LABELLING COMPLIANCE CONFIRMED 2026-06-30** (evidence-based audit vs gov.uk scheme + the inspector's email/code list): compliant — veal dropped (don't sell), mixed-origin already handled (separate bookings), country codes accepted, prep fully compliant, mince needs no "Reared in" (one optional "Origin:" wording nuance left per Hakan). **→ F-PROD-04 COMPLETE.** **🚀 UI PAUSE OVER (decision #16 satisfied) — Phase 1 screen-by-screen migration STARTS NOW, Hakan's chosen order: HACCP FIRST** (§5 groups it core-CCP → diaries/reviews → compliance/docs; §8 "which screen first" = HACCP, resolved). First sub-section = HACCP core CCP (`/haccp`, `/haccp/{cold-storage,process-room,delivery,mince}`); per-section requirements-audit-FIRST gate (decisions #8/#17), must pick up decisions #19 (label↔UI boundary) + #19a (collapsed-list dead-session error). **Architecture re-verified sound 2026-06-30** (boundary pin-tests 120/120; only the sanctioned 9 supabaseService + 2 leaflet adapter imports; rip-out test holds).
**Integration model = STEP BY STEP (decision #14):** main is sealed; 0a is in, 0b next, then each Phase-1 section (no longer accumulate-then-merge-at-end). **Reskin accepted + now LIVE (decision #15):** existing un-migrated screens carry the new palette/fonts on old layouts until Phase 1 rebuilds them — accepted as harmless on an internal staff tool. Weekly execution tracker: `docs/plans/2026-06-29-wc29-plan.md` (**`wc29`**). **After 0b (decision #16): pause UI → repo/docs cleanup on clean main → label printing F-PROD-04.**

```
LANE
  ✓ ui-cartographer   → docs/ui-current-state.md
  ✓ brand-prompt      → docs/design/2026-06-27-ui-system-brand-prompt.md
  ✓ Claude Design     → "MFS Operations Design System" (reviewed, build-ready)
  ✓ design pulled     → docs/design/MFS-Operations-Design-System.dc.html (on-disk source of truth)
  ✓ FORGE Phase 0     → 0a foundation ✓ SHIPPED (PR #93) · 0b core library ✓ SHIPPED (3 waves: #94/#95/#96) · 0c speculative (deferred) — PHASE 0 COMPLETE
  ● FORGE Phase 1+    → section by section (requirements-audit FIRST) — ACTIVE: HACCP core CCP first
  ○ ANVIL             → per phase/section test gate
  ◐ MERGE             → STEP BY STEP (decision #14): Phase 0 in; F-PROD-04 in; Phase-1 sections next
```

---

## 1 · Isolation & guardrails (do not break)

- **Working location:** the **main working directory** (`/Users/hakankilic/MFS-Operations`). The old worktree (`.claude/worktrees/ui-system-rebuild`, branch `worktree-ui-system-rebuild`) was **retired 2026-06-29** — its isolation reason (the now-sealed 16-day sprint running in parallel on `main`) is gone. Cut each section's feature branch straight from `main` here.
- **Merge IS unlocked** (since 0a, PR #93). `main` is sealed/certified. Each section ships via feature branch → PR → the `smoke` required check. "Work in main" = the main *dir*, NOT committing directly onto the `main` branch.
- **Presentation layer only:** `app/**` + `components/**`. Do NOT touch `lib/ports/**`, `lib/adapters/**`, `lib/wiring/**` (import existing singletons only), `middleware.ts`, auth/RLS, `app/api/**` route auth, `.github/**`, Vercel settings. New service/port/adapter ⇒ STOP, coordinate with Hakan.
- **No new vendor SDK in the UI** (Lego rule). The one approved new runtime dep is **Radix Primitives** (a11y engine). Test-only devDeps (jsdom, @testing-library/react, user-event, vitest-axe) are exempt, like `fake-indexeddb`.
- **No AI references** in commits, PRs, or code.

## 2 · Locked decisions

| # | Decision | Note |
|---|----------|------|
| 1 | Two-tier tokens: primitive → **semantic**; components bind to semantic ONLY | lint-enforced on new/touched code |
| 2 | Single source of truth = **CSS variables**; **Tailwind v3** reads them (NOT v4) | collapses today's double-declaration |
| 3 | **Light + unified dark** from day one (folds KDS `mfs-kds-*` + HACCP `slate-*`) | delivered: `[data-theme="dark"]` |
| 4 | A11y via **Radix Primitives** (single vendor); shadcn only as recoloured copy-in reference | WCAG AA min |
| 5 | **Buy** component-test stack: jsdom + @testing-library/react + user-event + vitest-axe | test-only |
| 6 | **Design comprehensive, build tiered** — full ~30 catalogue + speculative designed; build in waves | speculative = build-on-demand |
| 7 | **Migrate ALL 46 routes** onto the new system (old admin `_components/primitives.tsx` absorbed) | section by section |
| 8 | Per-section **requirements-audit FIRST** (incl. proactive new-business-logic suggestions); confirm each, never batch | see process doc / memory |
| 9 | Multi-format first-class; **default density = comfortable (touch-first)**, compact on desktop | Hakan confirmed |
| 10 | Display font = **Adieu** (licence confirmed OK to ship); Inter for body | resolves old "GTF Adieu unshipped" gap |
| 11 | Spacing via **density sizing system** (control/field/tap/stack/card tokens) ON TOP of Tailwind's default 4px scale (design uses ONE ruler — no custom spacing scale) | resolves spacing open item |
| 12 | **Fidelity, not inertness:** the `.dc.html` is the SINGLE source of truth — foundation gets the real design tokens (colours, Adieu+Inter fonts, dark, density) VERBATIM from day one. Do NOT preserve old colours / Plus Jakarta Sans. Existing screens shift look immediately — safe (isolated branch, no merge until overhaul done). Only "don't break" = the app must still BUILD. | 2026-06-28 — reverses the earlier "visually inert" plan at Hakan's direction |
| 13 | **Phase 0 = 3 FORGE PRs:** 0a foundation (fonts/tokens/Tailwind/Radix/test-stack) · 0b core library (~30 components) · 0c speculative (DEFERRED, build-on-demand). Phase 1+ stays screen-by-screen, audit-first. | 2026-06-28 — agreed at FORGE Frame |
| 14 | **Integration model = MERGE STEP BY STEP** — 0a merges to `main` once its cert clears, then 0b, then each Phase-1 section. Reverses the original "accumulate the whole overhaul on the branch, merge once at the end." | 2026-06-28 — Hakan's call (main is sealed, merge window open) |
| 15 | **Reskin ACCEPTED:** merging 0a's verbatim tokens to `main` re-skins every existing un-migrated screen with the new palette/fonts immediately (old layouts, new paint) until Phase-1 migrates them. Accepted as harmless/reversible on an internal staff tool. | 2026-06-28 — consequence of #12 meeting #14; Hakan chose option A |
| 16 | **Pause UI after Phase 0** (0a+0b merged), BEFORE any Phase-1 screen, to finalise **label printing** (F-PROD-04, critical). Repo/docs cleanup happens on clean main between 0b-merge and the pause. | 2026-06-28 — wc29 sequence |
| 17 | **GOVERNING RULE — "change once → every screen follows".** Every component-API decision is judged by: does it keep ONE global design system where changing one token/component updates every screen, with ZERO style knowledge leaking into screen files? Corollary, enforced per component: **NO style-leaking props** — no `className`, no `widths`/grid-template, no inline `style`, no hex; screens pass only **semantic intent** props (`accent`, `align`, `compact`, `hideBelow`, `last`, `size`). Components own ALL appearance via Tier-2 tokens. Also: **reject speculative seams** (don't build components with zero current consumers). Feeds directly into the §8 finalisation gate. | 2026-06-29 — Hakan's explicit lens at W2 Frame; memory `feedback_design_system_change_once` |
| 18 | **Wave-2 (Display) API decisions** (all chosen under #17): (a) **Table = real semantic `<table>`** (dot-notation `Table`/`.Head`/`.Body`/`.Row`/`.HeaderCell`/`.Cell`, sizing via `align`+`hideBelow` semantic props) — NOT a grid faux-table with `widths`; accepted that the Phase-1 admin re-point becomes a rewrite, to buy the no-leak property. (b) **SegmentedControl only** (pill value-toggle, `role=group`+`aria-pressed`) — NO Radix Tabs this wave (zero panel consumers = speculative). (c) **Three status components** (Badge + StatusPill + SyncDot), not one polymorphic Badge. (d) Carried from W1: ARIA labels = optional props w/ English defaults; icons = caller `ReactNode` (no icon lib). | 2026-06-29 — locked at W2 Gate 1; SHIPPED PR #95 `9b91885` |
| 19 | **Label-printing ↔ UI boundary (answers Hakan's "how do label changes hit the UI?").** The printed label is server-side print HTML (`lib/printing/html.ts`, its own `@page` CSS) — **NOT** part of the design system; restyling a label never touches Phase 1, and Phase 1 never touches labels. So: a label/regulatory field **already captured in the DB** = a **label-render-only** change, safe anytime (e.g. Pass 3 real-allergens). A field needing **NEW staff entry** = an **app-screen** change → add capture via existing `components/ui/` components (no bespoke styling), let the normal Phase-1 sweep restyle it; do NOT hand-paint a screen for it (per #17). Applies to the deferred beef-labelling regulatory job. | 2026-06-29 — Hakan asked during F-PROD-04 Pass 1 |
| 19a | **Deferred F-PROD-04 Pass-1 follow-up for Phase 1** (code-critic 🟡 #1, PR #98): on the delivery COLLAPSED-LIST print strip (`app/haccp/delivery/page.tsx`), a dead-session print error routes to the page-level `submitErr` (entry-form area) so on a long list the "log in again" message renders off-screen above the tapped row. NOT silent (printing IS blocked, login page never prints — safety met); the modal + mince paths show it next to the button. Proper fix = per-row error state = bespoke screen logic → handle when this screen is migrated in Phase 1. | 2026-06-29 — parked to Phase 1 per #17 |
| — | Deferred: Tailwind v4 upgrade · Style-Dictionary/DTCG pipeline | revisit only when a real reason appears |

## 3 · Design system handover (from Claude Design)

- **Project:** "MFS OPS NEW" — `projectId 0e28a094-d725-42bd-8858-cd469b21a42d` (read via DesignSync MCP; `/design-login` for auth).
- **Main file:** `MFS Operations Design System.dc.html` (~264 KB, self-contained spec).
- **Assets in project (pull into repo at Phase 0):** `fonts/Adieu-Regular.otf`, `fonts/Adieu-Light.otf`; `assets/logo-{navy,orange,white}.svg`, `assets/star-icon-{navy,orange,sand,white}.svg`.
- **Review verdict (2026-06-28): STRONG / build-ready.** Covers the full catalogue + speculative set; two-tier tokens with complete semantic layer (action/surface/text/border/status/sync/focus + lazy-resolving domain status aliases); light + unified dark; density modes (comfortable/compact); breakpoint scale; WCAG-AA contrast pairings; i18n + prefers-reduced-motion; cross-cutting "seams" (PWA/offline) carried. Both open items (font, spacing) resolved.
- The `.dc.html` is the **source of truth FORGE builds against** (pull fresh via DesignSync each session; do not hand-edit a stale copy).

## 4 · Phase 0 — design-system foundation (build FIRST, before any screen)

**Split into 3 FORGE PRs** (decision #13): **0a** = the foundation rows below minus the component library (fonts/assets · token CSS · Tailwind rewire · Radix · test-stack · lint rule · token+stack tests) · **0b** = the **core** component library row · **0c** = the section-driven + build-on-demand rows (DEFERRED). The active 0a plan lives at `docs/plans/archive/2026-06-28-ui-phase-0a-foundation.md`. Tokens are implemented VERBATIM from `docs/design/phase0a-foundation-tokens.reference.css`.

**0a status (✓ DONE, awaiting merge):** token CSS · Tailwind rewire · Radix · test-stack · semantic-tokens lint · token-compile + stack tests — all ✓ on `feat/ui-phase-0a-foundation`. Fonts ✓ (Adieu in repo); **7 brand SVGs DEFERRED** (see change log). **0b** = core component library (next). **0c** rows (section-driven + build-on-demand) = DEFERRED. Review: `docs/reviews/2026-06-28-ui-phase-0a-foundation-review.md`. Cert (PENDING): `docs/anvil/2026-06-28-ui-phase-0a-foundation-cert.md`.

| Task | Status |
|------|--------|
| Pull Adieu fonts + logo/star assets into repo (`public/fonts`, `public/…`) | ○ |
| Tokens CSS: Tier 1 primitives + Tier 2 semantic, light + `[data-theme="dark"]` + density (`[data-density]`) — one source of truth | ○ |
| Tailwind v3 config reads the semantic CSS vars (kill the double-declaration) | ○ |
| Add Radix Primitives (runtime dep, justified) | ○ |
| Add component-test stack (jsdom + RTL + user-event + vitest-axe); vitest config | ○ |
| Build **core** library (semantic-tokens only), absorbing `app/dashboard/admin/_components/primitives.tsx`: Button, IconButton, fields (TextField/Textarea/Select/Checkbox/Radio/Toggle), FormField, PIN Keypad, Picker; Card, KpiTile, ListRow, Table, SectionLabel/PageHeading/CardHead, Tabs/SegmentedControl; Modal/Dialog (centred+sheet), Banner/Alert, Spinner, EmptyState, Badge/StatusPill, SyncDot, Popover/DropdownMenu; AppHeader/BottomNav/MoreDrawer/DesktopSidebar/NavItem | ○ |
| Lint rule: semantic-tokens-only in components (new/touched code) | ○ |
| Tests: token compile + per-component keyboard/focus/ARIA/contrast (vitest-axe) | ○ |
| **section-driven (design now, build later):** FileUpload, Accordion, Toast, Skeleton, ProgressBar, Tooltip, Avatar; HACCP TileState; Map shell restyle; Print/label | ○ |
| **build-on-demand (speculative, designed only):** date/range pickers, calendar, command palette, rich-text, carousel, stepper, data-grid, kanban, timeline, notification center, combobox, slider, rating, tree, breadcrumb, pagination | ○ |

## 5 · Phase 1+ — section-by-section migration (46 routes)

**Per-section loop (HARD gate, never batch):** (a) requirements audit FIRST — document rules/edge cases, confirm correctness, **suggest new business logic with rationale**, lock; (b) redesign on the new system; (c) test to depth (each rule → a test; critical flows → E2E). Confirm with Hakan before the next section. **Sequencing LOCKED (Hakan): HACCP FIRST** — HACCP core CCP → HACCP diaries/reviews → HACCP compliance/docs, then the rest. (The §8 "which screen first" open item is now resolved.)

| Section | Routes | Status |
|---------|--------|--------|
| Auth & entry | `/login`, `/` | ○ |
| Orders | `/orders`, `/orders/new`, `/orders/[id]`, `/orders/[id]/edit`, `/kds` | ○ |
| Dispatch / logging | `/dispatch`, `/screen2`, `/screen3` | ○ |
| Sales / CRM | `/visits`, `/complaints`, `/compliments`, `/pricing`, `/cash` | ○ |
| Routes / logistics | `/routes`, `/runs`, `/driver`, `/map` | ○ |
| Admin dashboards | `/dashboard/admin`, `/admin`, `/admin/{at-risk,commitments,discrepancies,prospects,visits}` | ○ |
| HACCP — core CCP | `/haccp`, `/haccp/{cold-storage,process-room,delivery,mince}` | ✅ **SECTION COMPLETE — hub ✅ SHIPPED (#106/#107); `/haccp/cold-storage` ✅ SHIPPED (#108 `d90c0aa`); `/haccp/process-room` ✅ SHIPPED (#109 `20e5b01`); light-refresh Unit 1 ✅ SHIPPED (#110 `f02e96c`); COLOUR-PAIRING SYSTEM · Unit 2 ✅ SHIPPED (#111 `b30a0e9`) — F-TD-40 closed (text AND border un-inerting), measured pairing law + surface contexts + contrast-regression vitest, hub repaint (bold-navy header, alarm flip preserved + forced-alarm E2E); **`/haccp/delivery` (→"Goods In") ✅ SHIPPED (#112 `63db7fd`, 2026-07-02)** — kit rebuild on the pairing system + DB-driven legislation-verified CCP-1 thresholds (poultry ≤4/4–5/>5 legal fix) + immutable audit + admin editor; **`/haccp/mince` ✅ SHIPPED (#113 `b5eb83e`, 2026-07-02)** — kit rebuild + DB-driven law-verified CCP-M thresholds (9 rows: 6 temp channels + per-species kill-days; DISPLAY-ONLY amber — CA paperwork unchanged, deliberate divergence from goods-in) + immutable audit + admin editor + 4 bug fixes (timesep CA persist+register · header filter · dual-channel combined causes · submitErr dedupe) + minus key on all pads.** Order: ~~hub~~ → ~~cold-storage~~ → ~~process-room~~ → ~~light U1~~ → ~~pairing U2 (hub)~~ → ~~delivery(→"Goods In")~~ → ~~mince~~ — ALL DONE |
| HACCP — diaries/reviews | `/haccp/{cleaning,product-return,calibration,reviews,annual-review,people,training}` | ○ |
| HACCP — compliance/docs | `/haccp/{allergens,recall,product-specs,food-fraud,food-defence,audit,documents,documents/[ref],visitor,admin}` | ○ |

## 6 · Testing approach

Belt-and-braces where risk is real; TDD red-green-refactor; right layer (unit=logic, integration=connections, component+a11y=UI via the bought stack, E2E=critical flows). Build on the existing repo suite, not a parallel one. Include ≥1 **Android-webview smoke** (Capacitor APK; web-only E2E insufficient). Suite green before any phase/section is "done". Right-size to blast radius.

## 7 · Cross-cutting concerns to preserve (inventory §6)

PWA/offline (SyncDot + RecentActivity), Android/Capacitor + Sunmi printing, PIN keypad, Leaflet maps (quarantined; known re-mount bug), print/PDF/Excel surfaces, EN/TR i18n (route new text through `t()`; TR audit folded into each section's step (a)), role-gated rendering (6 roles).

## 8 · Open items / risks

- **FINALISATION GATE (hard, before the UI overhaul is declared "done"): full token-binding audit.** Every shipped screen must bind to the semantic design tokens — colours, typography (fonts), spacing, radii — with NO hardcoded values anywhere in `app/**` or `components/**`. Acceptance test: **changing the single token source (`app/tokens.css` / the `next/font` declaration) must re-style every screen with zero per-page edits** (the typography rip-out test = "swap the font in one place → all pages change"). The 0a `semantic-tokens-only` ESLint rule + `token-resolve` guard enforce this on new/touched code; this gate is the WHOLE-TREE sweep that proves no legacy holdout (un-migrated screen, leftover hex, hardcoded font-family) escaped. Run it after the last Phase-1 section, before sign-off. 🗣 One paint supply, every room wired to it — prove it by changing the supply once and watching the whole building change, with no room missed.
  - **Component-level corollary (decision #17, being built in NOW):** the gate is far easier to pass if components never accept style-leaking props in the first place. Every `components/ui/` component built from W2 on takes **semantic intent props only** (no `className`/`widths`/inline-`style`/hex). So when Phase-1 screens are re-pointed onto these components, the screens *can't* hold style — the audit becomes a confirmation, not a cleanup. Verify at the gate that no screen passes raw style into a `components/ui/` component.
- ~~Sequencing of Phase 1 sections (which screen first)~~ — ✅ RESOLVED 2026-06-30: **HACCP FIRST** (core CCP → diaries/reviews → compliance/docs), then the rest.
- Leaflet interior can't be fully tokenised; restyle around it (accepted).
- Map "container already initialized" re-mount bug — deferred; address when Routes/Map section comes.
- Large scope (46 routes, ~22 HACCP) — expect many gated sessions; this roadmap is the tracker.

## 9 · Change log

- **2026-07-02 (/haccp/mince — ✅ SHIPPED, PR #113 squash `b5eb83e`; full FORGE→ANVIL; 🏁 HACCP CORE-CCP SECTION COMPLETE)** — fifth and LAST Phase-1 core-CCP screen: "Mince & Meat Prep" (CCP-M1/M2/MP1/MP2, 3 sub-forms) recomposed on the kit + pairing system. **Headline = DB-driven, legislation-VERIFIED CCP-M thresholds:** Frame verified every band against GB assimilated Reg 853/2004 Annex III Sec V Ch III (web-fetched, quoted) — **all current numbers CONFIRMED compliant** (input ≤7 red meat · mince out ≤2 · prep out ≤4 · frozen ≤-18 · kill ≤6d lamb/beef); the one legal gap found — chilled boned vac-packed beef/veal carries a hard **15-day** mincing ceiling (pt 2(b)(iii), no import/origin/shelf-life exemption; "no kill date needed" is TRUE only for the FROZEN route, which also bans re-freezing output) — **Hakan explicitly kept imported_vac = NO app-enforced limit** (manual/operator control; deviation + frozen-route nuance written into DOCUMENT_CONTROL §4, red-meat-justification precedent). New `haccp_mince_thresholds` (9 rows: 6 temp channels + 3 per-species kill-day rows; `kind` CHECK makes kill-day rows structurally amber-less; NO active column) + immutable `haccp_mince_threshold_audit`; admin writes double-locked (route isAdmin + DB `is_admin()`, pgTAP 020); admin CCP-M editor section on /haccp/admin (temps + kill-days). **Amber = 1°C grace bands on all 6 temp channels but DISPLAY-ONLY (Hakan's pick, deliberate divergence from goods-in's conditional-accept amber): CCA popup + CA register still fire on anything over pass** — enforced structurally by twin functions in single-source `lib/domain/mincePrep.ts` (`minceTempStatus` colour vs `minceTempPass` paperwork; killed the page/service duplicate band tables) and pinned at 4 layers. Poultry-less confirmed BY DESIGN (no poultry mince/prep on site; poultry would need ≤4°C input + 3-day kill law). **4 bug fixes:** timesep corrective-action text was captured but silently discarded → persisted + NEW timesep CA-register builder; timesep history header now honours the date filter; dual-channel CCA popup offers a combined deduped cause list (was input-only); duplicate submitErr render removed. Guard 🟡 → minus key restored on ALL pads (`c8f8533`, E2E-pinned −1°C chilled entry). **Guard: NO BLOCKERS; implementer ran ~490k tokens → fresh critic ran the 4 late-context checks (plan completeness / test weakening / behaviour loss / hard guards) — ALL PASS.** ANVIL CLEARED, ZERO iterate loops, zero real-code bugs: unit 3326 · integration 571 · pgTAP 293 · E2E 20 local + 106 preview green (only the 2 accepted signature reds) · 21 browser-tap shots incl. admin edit→self-updating band copy→restore (audit rows DB-verified) · rollback script written AND tested. Migration additive (no PITR), applied to prod via Supabase MCP, all 9 seed rows verified in prod. Prod `b5eb83e` READY on mfsops.com, smoke 3/3 → 200 (incl. /haccp/mince). Cert `docs/anvil/2026-07-02-haccp-mince-unit-cert.md`; review `docs/reviews/2026-07-02-haccp-mince-review.md`; plan archived. BACKLOG: F-TD-44/45 extended to the mince table; new **F-TD-46** (pgTAP runner picks up `_helpers.sql` → cosmetic FAIL summary line). **Reusable lesson: the law check pays for itself even when nothing is broken — this time it confirmed compliance AND converted a "we were told no limit" folk rule into a precise documented deviation (chilled = 15 days; frozen route = the true no-limit case).** **NEXT = Phase-1 continues past core-CCP: diaries/reviews group (per §5), or Hakan's pick.**
- **2026-07-02 (GOODS IN / CCP 1 — ✅ SHIPPED, PR #112 squash `63db7fd`; full FORGE→ANVIL)** — fourth Phase-1 screen: `/haccp/delivery` recomposed on the kit + pairing system (labels-only rename to "Goods In"; route unchanged). **Headline = DB-driven, legislation-VERIFIED CCP-1 temperature thresholds:** Frame verified every band against the live law (Reg 853/2004 Annex III Sec I/II + Quick-frozen Foodstuffs Regs 2007) and found **poultry passing at ≤8°C against a 4°C legal limit** — fixed to ≤4 pass / 4–5 amber (Hakan's documented 1°C grace band) / >5 reject; red meat stays ≤5/5–8/>8 by Hakan's explicit decision (1°C above Reg 853's 7°C — written justification in DOCUMENT_CONTROL §4, which was itself corrected: its "frozen ≤-12" was the QFF retail-cabinet exception, not a receipt rule). New `haccp_goods_in_thresholds` (11 rows incl. legacy keys — fail-closed grading has no hole; NO active column, the process-room Guard lesson made structural) + immutable `haccp_goods_in_threshold_audit`; admin writes double-locked (route isAdmin + DB `is_admin()`, pgTAP 019); admin editor section on /haccp/admin. Band rule single-sourced in `lib/domain/goodsIn.ts` (client + server import one module — killed the duplicate that let the poultry bug live). Kit `NumberPad` upgraded (Guard 🟡 fix): minus + decimal together via a sign-toggle row — also healed process-room's latent minus-drop and frozen_beef_lamb's untypeable negatives. Green/amber caging repaint with a recorded amber EXCEPTION (Quick-ref explainer boxes + CCA urgent header — spec §1). **Guard: NO BLOCKERS; the implementer ran ~500k tokens so a fresh critic ran 4 late-context checks (plan completeness / test weakening / behaviour loss / hard guards) — ALL PASS.** ANVIL CLEARED: unit 3266 · integration 561 · pgTAP 276 · E2E 100 local + 100 preview (2 documented signature reds, both evidenced) · 39 browser-tap shots · zero product-code fixes (both iterate loops were tests). Migration additive (no PITR), applied to prod via Supabase MCP, seed verified in prod. Prod `63db7fd` READY on mfsops.com, smoke 3/3 200. Cert `docs/anvil/2026-07-02-goods-in-ccp1-unit-cert.md`; review `docs/reviews/2026-07-02-goods-in-ccp1-review.md`; spec + execution plan archived. New BACKLOG **F-TD-44/45**. **Reusable lesson: a screen rebuild is where legal-limit bugs surface — verify the numbers against the actual regulation at Frame; and a long-context builder is acceptable IF a fresh critic afterwards audits plan completeness, test weakening and behaviour loss.** **NEXT = `/haccp/mince` (last core-CCP screen).**
- **2026-07-02 (COLOUR-PAIRING SYSTEM · Unit 2 — ✅ SHIPPED, PR #111 squash `b30a0e9`; full FORGE→ANVIL)** — Hakan reviewed the hub post-Unit-1 (grey-on-grey buttons, black text on navy = brand-BANNED p12 pairing) and directed: **stop using colour combos blindly — design a background×element×text rule system so every migrated screen is right by construction.** Spec researched (brand PDF pp8–16 re-read + WCAG contrast MEASURED for every pairing; the scripts became a permanent vitest), decisions locked: **maroon-500 headings on cream** (the brand book's own typography treatment; navy = the frame), **orange-500 + ink-900 primary buttons** (white-on-orange FAILS body text at 3.3 — measured), per-action `-fg` tokens replacing blanket `text-on-action`, sand takes BLACK text only (Hakan caught white-on-sand ≈2.7 himself), rules live in CODE only (Claude Design stays a Tier-A mockup tool + optional record — spec §12). **Three phases, one PR: (0)** F-TD-40 CLOSED properly — semantic text colours → `theme.extend.textColor`, borders → `borderColor` (implementation discovered the whole BORDER namespace was inert too: 78 sites silently on preflight grey); **(1)** pairing tokens + 4 `data-surface` context blocks (canvas/bold-navy/alarm/bold-maroon) that re-scope the semantic vars — illegal pairings unrepresentable; measured token moves (link→orange-700, focus-ring→orange-600, NEW `--border-input` ink-400, ghost borders navy-300); NEW `tests/unit/design/contrast-pairings.test.ts` (36 legal pairings + 4 banned negatives, hexes PINNED to live tokens.css) + `tailwind-namespace-collision.test.ts` incl. the `extend.colors` side door; **(2)** hub repaint (HomeScreen + LoginDoor) — bold-navy `ScreenHeader` (now a real `<header>` landmark), alarm flip preserved as a `surface` ternary on the SAME `alarm.isAlarming` boolean (alarm brain byte-identical to main = hard acceptance criterion, verified by Guard independently), StatusTile `neutral` → white card + navy icon (grey wash demoted to disabled-only), NEW spec 31 forced-alarm E2E asserting COMPUTED white-on-red ≥4.5 in both envs. **Guard: NO BLOCKERS + 5 🟡 fixed on-branch** (headline catch: the orange Admin button on the red alarm surface at 1.54:1 — banned by the PR's own law → ghost-inverse while alarming). ANVIL CLEARED: unit 3214 · integration 554 · E2E local 95/97 + preview 94/97 (all 3 reds EVIDENCE-proven pre-existing/environmental: map specs 05/06 fail identically on `main` → F-TD-41; preview weekly-review = F-INFRA-08 consumed slot, SQL-confirmed) · spec 31 2/2 both envs · 17 browser-tap shots incl. KDS + /orders unchanged controls. Prod `b30a0e9` READY on mfsops.com, smoke 3/3 200. New BACKLOG **F-TD-41/42/43** (43 = hub brand-mark restore-vs-accept, Hakan pending). Cert `docs/anvil/2026-07-01-colour-pairing-unit2-cert.md`; review `docs/reviews/2026-07-01-colour-pairing-unit2-review.md`; spec + execution plan archived. **Reusable lesson: brand-legal ≠ readable — every pairing needs BOTH gates (brand book AND measured contrast per role), and encoding the law as tokens + contexts + a maths test beats policing it in review.** **NEXT = `/haccp/delivery` (→"Goods In") built on the pairing system.**
- **2026-07-01 (GLOBAL DESIGN-TOKEN REFRESH · Unit 1 — ✅ SHIPPED, PR #110 squash `f02e96c`; full FORGE→ANVIL)** — Hakan disliked the *look* of the shipped HACCP screens; root cause was diagnosed against the **MFS Wholesale Brand Guidelines PDF** (see memory `reference_brand_guidelines`), not guesswork. **Two defects found + brand-validated:** (a) "blue-on-blue" (navy button on navy) = literally a brand-BANNED dark-on-dark misuse, an artifact of the DARK theme; (b) "pinkish banner" = an invented washed-out maroon tint the brand never uses. **KEY INSIGHT:** the brand palette has NO green/amber — the HACCP screens leaned on invented ones, which is why they read off-brand; the brand's canvas (Soft Neutral #EDEAE1) already IS the app's light skin. **Decisions locked with Hakan (all recommended):** direct-to-code (not via Claude Design — the PDF is already the authoritative blueprint); brand-red UNIFICATION (all error/danger/deviation/fail reds → one brand Mediterranean Red `#FF3300`, retiring the invented scarlet crimson — APP-WIDE on the light theme, not HACCP-only); green/amber CAGED to temperature pass/warn/fail only (written rule in CLAUDE.md); bold-navy kit `ScreenHeader` (approved navy+soft pairing); flip HACCP to LIGHT (remove the dark opt-in + delete `ThemeLock`). **SPLIT into 2 units** to de-risk. **Unit 1 (shipped):** tokens red-unify + light flip + `ScreenHeader`/`ghost-inverse` kit + cold-storage & process-room headers + a token-purity lint guard. **ANVIL caught a REAL bug** (good): `text-inverse` was an INERT Tailwind class (colour group named `text` → only `text-text-inverse` compiled) → dark text on the navy header; hidden until now because the old dark theme's default text was already light. Fixed at root with a one-line top-level `colors.inverse` alias (audited: all 12 `text-inverse` usages sit on dark surfaces → no white-on-white; KDS dark unaffected; this also keeps the hub's red overdue-alarm legible in light). New BACKLOG **F-TD-40** (the rest of the inert `text-*` colour family + `bg-inverse`/`border-inverse` foot-gun — deliberate follow-up). ANVIL re-run CLEARED: unit 3138 · integration 554 (confirmed unaffected — config-only) · @critical E2E 93 pass on the prod-build preview (specs 13/16 white-on-navy flipped red→green; spec 29 non-HACCP brand-red + AA) · red-text-on-red-soft AA 5.85:1. No migration/RLS/PITR. Prod deploy `f02e96c` READY on mfsops.com, site 200. **Hakan looked at cold-storage + process-room on prod and APPROVED the light + brand-red direction (2026-07-01)** → Unit 2 + `/haccp/delivery` proceed on this look. Cert `docs/anvil/2026-07-01-light-design-refresh-unit1-cert.md`; review `…-review.md`; plans archived. **Reusable lesson: ground a design refresh in the real brand book (approved pairings, forbidden combos) — it turns "I don't like it" into precise token moves and catches brand VIOLATIONS (dark-on-dark) the eye only feels.** **NEXT = Unit 2 (hub `/haccp/page.tsx` bold-navy header, PRESERVING the alarm-red overdue flip + forced-alarm E2E), then continue `/haccp/delivery` on the new look.**
- **2026-07-01 (Phase 1 · `/haccp/process-room` — ✅ SHIPPED, PR #109 squash `20e5b01`; full FORGE→ANVIL, clean first pass)** — Third Phase-1 screen. **Requirements-audit FIRST** (Explore agent inventoried the live two-form screen: CCP-3 temps AM/PM + SOP-1 diary opening/operational/closing) → deltas locked with Hakan. **DB-DRIVEN FSA-ALIGNED THRESHOLDS (the headline):** new `haccp_process_room_thresholds` table (seeded Product core 4/7, Room ambient 12/15) + immutable `haccp_threshold_audit`, replacing the hardcoded ≤4/≤12/>15 logic — closes deferred audit item **B7**. **FSA verification (Hakan asked to design per FSA):** checked `DOCUMENT_CONTROL.md` §4 (≤4°C product / ≤12°C room, Reg 853/2004) against live FSA guidance — Reg 853/2004 = cutting-room ambient ≤12°C, meat core ≤7°C red / ≤3°C offal / ≤4°C poultry. Product gained an **FSA-aligned amber band 4–7** (≤4 green in-house target, 4–7 amber below legal max, >7 critical breach) — stops crying-wolf on a legally-fine 5°C; room kept its 12–15 amber / >15 critical (defensible as the "alternative system" early-warning now product core is the hard gate). **Admin-editable** via `/haccp/admin` (mirrors the Supplier register), **admin-only proven twice** (route `isAdmin` + DB RLS `is_admin()`; pgTAP proves a non-admin write is DENIED), **audit-logged** (who/when/old→new, immutable), with an **on-save reminder to update `DOCUMENT_CONTROL.md` + retrain** (preserves the compliance control that hardcoding used to enforce) — DOCUMENT_CONTROL §4/§7 updated in the same PR. Also: shared `lib/domain/processRoom.ts` (band helper consumed by BOTH screen + server — one better than cold-storage; kills the client/server cause-list drift via `PROCESS_ROOM_CAUSES`), server-side range guard (`isProcessRoomTempInRange`), kit `NumberPad`/`Modal` + semantic tokens + dark theme, dead code removed, past-dates LEFT AS-IS (explicit). **Guard = FIX-THEN-SHIP** (no 🔴; 2 🟡 fixed on-branch: an admin "deactivate a threshold" fail-OPEN hole → removed the toggle for the two fixed points + made `resolveProcRoomThresholds` fail CLOSED + a pinning test; audit summary now lists only changed fields). **ANVIL CLEARED:** unit 3134 · integration 554 (thresholds 43) · pgTAP 018 (non-admin write denied, audit immutable) · E2E spec 16 ported the `enterSession` race-proof pattern (F-INFRA-08 template) — preview 92 passed / 0 failed / 1 cold-start-flaky-green-on-retry on a **wiped** preview DB. Prod migration additive (no PITR) applied via authenticated Supabase; deploy READY on mfsops.com; prod smoke page 200 + readiness 200. New 🔵 BACKLOG follow-ups: F-TD-37 (non-transactional threshold update+audit window), F-TD-38 (protocol guidance text still duplicated screen↔server), F-TD-39 (seed band numbers as view-layer fallbacks — cosmetic), plus widen the spec-16 cold-start timeout. **NEXT = `/haccp/delivery` (→"Goods In").**
- **2026-07-01 (Phase 1 · `/haccp/cold-storage` — ✅ SHIPPED, PR #108 squash `d90c0aa`; the "prod-build-only regression" was a MISDIAGNOSIS — it was a test-harness race, NOT a code bug)** — The earlier BLOCKED entry (below) was wrong on the cause. Root-caused by finally doing what the checkpoint said: run the specs DIRECTLY against the live preview with tracing + query the preview DB. **Real cause:** cold storage is once-per-session-per-day and the **shared Vercel-preview Supabase DB is never reset between runs**, so after the first run a session is already submitted → the screen is **read-only** (no comments field / no Submit button). The specs' "already submitted?" guards **raced the client-side `loadReadings` fetch** (checked for the read-only banner before it rendered, missed it, drove into the read-only page → 30s timeout); `168` also raced its final once-per-session recheck (post-submit `loadReadings` auto-selects the freed PM, overriding the AM click). **The failing spec-set literally shifted run-to-run with which sessions the DB happened to hold** — the tell it was DB-state, not code. **PROOF the prod build is correct:** a clean-DB preview run saved AM Beef Chiller 12 °C critical via the "Defrost cycle" cause and reached "Session submitted" (confirmed by DB query). **Fix (`62ec87b`, TEST-ONLY):** an `enterSession()` helper waits for the units to render before selecting a session, then gracefully early-returns on an already-submitted session (the route→service→repo save is authoritatively pinned at integration). Verified on the LIVE preview both ways — **clean DB 5/5 (submits genuinely exercised), dirty DB 5/5 (graceful early-return)** — deterministic either way; CI `smoke` green (run 28511169674). The `9103fed` shared-`Modal` pointer-events hardening from the prior session was chasing a ghost (kept as a harmless reviewed guard, NOT the fix). Cert now ✅ CLEARED FOR PRODUCTION. New BACKLOG **F-INFRA-08** (reset the preview cold-storage/once-per-session data at smoke start so those E2E flows always genuinely exercise, not degrade to a read-only-banner assertion on a dirty DB). **Reusable lesson: a "prod-build-only" failure on a shared preview is FIRST a DB-state/test-harness suspect — trace the real environment + query the DB before hypothesising a code regression.** NEXT = `/haccp/process-room`.

- **2026-07-01 (Phase 1 · `/haccp/cold-storage` — BUILT + Guard-clean + local-green but 🔴 BLOCKED on a prod-build-only preview failure; PR #108 open, NOT merged) [SUPERSEDED by the entry above — diagnosis was WRONG]** — The rebuild is DONE and passes everything locally, but the Vercel **prod-build preview** `@critical` smoke reproducibly fails **2 cold-storage specs** — `tests/e2e/13-haccp-cold-storage.spec.ts:74` (the EXISTING regression happy-path submit) + `tests/e2e/13-haccp-cold-storage-phase1.spec.ts:168` (Defrost-saves) — both a `locator.click` 30s timeout on **Submit**, so "Session submitted" never appears. The regression spec is UNCHANGED and passes on `main`, so this PR's page rebuild introduced a **real prod-build-only regression** (local dev + a local prod build both PASS — the failure only reproduces on the preview). **PROCESS MISS to fix next session:** all fix attempts so far were HYPOTHESES (Radix Dialog `body{pointer-events:none}` race from the `{cond && <Modal open/>}` unmount-while-open pattern) — a shared-`Modal` hardening was written + verified green on a LOCAL prod build (`9103fed`) but did NOT fix the preview (identical failure persists). **We never captured a trace from the actual failing preview environment** — that is the next step. NEXT: on a fresh context, run the 2 specs DIRECTLY against the live preview URL with tracing (`npm run test:e2e:preview -- <preview-url> --unprotected -g "cold storage"`), WATCH the real hang (frozen element / DOM state), find the TRUE cause, then fix with evidence. Do NOT blind-fix + 8-min-smoke again. **All the local layers are green** (unit 3107 · integration 550 +8 incl. the Defrost-saves bug-fix proof · local E2E 5/5 dev + local prod build). Branch `feat/haccp-cold-storage-ui-phase1` HEAD `9103fed`. Cert `docs/anvil/2026-06-30-haccp-cold-storage-ui-phase1-cert.md` = BLOCKED (not cleared). Detail: [[project_ui_rebuild_progress]] + the checkpoint. **Option on the table (Hakan): if the diagnosis stalls, ABORT PR #108 and shelve cold-storage — the hub is already live.** The rebuild is DONE and passes everything locally, but the Vercel **prod-build preview** `@critical` smoke reproducibly fails **2 cold-storage specs** — `tests/e2e/13-haccp-cold-storage.spec.ts:74` (the EXISTING regression happy-path submit) + `tests/e2e/13-haccp-cold-storage-phase1.spec.ts:168` (Defrost-saves) — both a `locator.click` 30s timeout on **Submit**, so "Session submitted" never appears. The regression spec is UNCHANGED and passes on `main`, so this PR's page rebuild introduced a **real prod-build-only regression** (local dev + a local prod build both PASS — the failure only reproduces on the preview). **PROCESS MISS to fix next session:** all fix attempts so far were HYPOTHESES (Radix Dialog `body{pointer-events:none}` race from the `{cond && <Modal open/>}` unmount-while-open pattern) — a shared-`Modal` hardening was written + verified green on a LOCAL prod build (`9103fed`) but did NOT fix the preview (identical failure persists). **We never captured a trace from the actual failing preview environment** — that is the next step. NEXT: on a fresh context, run the 2 specs DIRECTLY against the live preview URL with tracing (`npm run test:e2e:preview -- <preview-url> --unprotected -g "cold storage"`), WATCH the real hang (frozen element / DOM state), find the TRUE cause, then fix with evidence. Do NOT blind-fix + 8-min-smoke again. **All the local layers are green** (unit 3107 · integration 550 +8 incl. the Defrost-saves bug-fix proof · local E2E 5/5 dev + local prod build). Branch `feat/haccp-cold-storage-ui-phase1` HEAD `9103fed`. Cert `docs/anvil/2026-06-30-haccp-cold-storage-ui-phase1-cert.md` = BLOCKED (not cleared). Detail: [[project_ui_rebuild_progress]] + the checkpoint. **Option on the table (Hakan): if the diagnosis stalls, ABORT PR #108 and shelve cold-storage — the hub is already live.**
- **2026-06-30 (Phase 1 · `/haccp/cold-storage` — requirements AUDITED + locked, Tier B; build NEXT)** — Read-only audit (Explore agent) of the live CCP-2 fridge/freezer temp-log screen. It logs AM/PM temps per data-driven chiller/freezer unit (thresholds DB-driven), auto-classifies pass/amber/critical, and forces a corrective-action capture (cause/disposition/recurrence) on any deviation; once-per-session DB-enforced; success→hub redirect; data path already clean (page→route→service→port→Supabase adapter, so rebuild is presentation-only). **3 decisions locked with Hakan:** **(🔴 bug)** the corrective-action cause list mismatches — client offers 8, server `VALID_COLD_STORAGE_CAUSES` accepts only 6, so "Defrost cycle — scheduled temperature rise" + "High ambient room temperature" are REJECTED on save → **fix = expand the server to accept all 8** (both are legitimate causes). **(+safety)** add **number-pad entry sanity bounds** (~−40 to +30°C, exact TBC at build) so a fat-finger can't log nonsense (pass/amber/critical judging unchanged). **(Tier B — build straight)** no Claude Design mockup; rebuild on `components/ui/` + tokens, judged on the live dark preview. **Build-approach (decide-and-proceed):** the custom number-pad + the corrective-action/quick-ref pop-ups are hand-rolled `fixed inset-0` divs today → rebuild as **kit components** (a reusable `NumberPad` in `components/ui/` per the new governance rule + ADR-0014 Rule 3; pop-ups → kit Modal/sheet); replace all hardcoded hex (incl. ~15× `#EB6619`) with tokens; drop the dead `unit_type` the client sends but the server ignores. i18n stays hardcoded EN (F-UI-I18N-01). **PRESERVE** all behaviour (AM/PM, per-unit entry, classification, corrective-action flow, once-per-session guard, today-only date rule, role gating, handbook/quick-ref links). Noted-not-fixed (out of scope, candidate backlog): duplicate hardcoded AM/PM cutoff in `HaccpReportingService.ts` (`:136-137` vs `:1241-1242`). **Next: FORGE (Frame light — spec locked via the audit gate).**
- **2026-06-30 (Phase 1 · `/haccp` hub — post-ship touch-ups + DESIGN-SYSTEM GOVERNANCE RULE, ✅ SHIPPED PR #107 `4c8305c`)** — Three fixes from Hakan's live look + a durable rule. **Fixes:** (1) StatusTile `onPointerDown`→`onClick` so scrolling a tile no longer navigates (+ keyboard activation restored); (2) **force ALL of HACCP to dark mode** — Hakan approved dark in Claude Design but the build shipped light; `data-theme="dark"` on the kiosk shell + a `ThemeLock` client island that locks `document.documentElement` dark while HACCP is mounted (so Radix-portaled overlays — PIN modal/help — inherit dark) and restores on exit; (3) wide `MfsLogo` wordmark in the HACCP header → compact square `MfsIcon` (official star asset, top-left). **🔑 NEW GOVERNANCE RULE (Hakan: "we never wire reusable UI directly into a page; everything important comes from the design system"):** `MfsLogo` moved INTO `components/ui/` (it was wrongly outside), both brand marks barrel-exported, 3 importers re-pointed; **a written rule added to `CLAUDE.md`** (every shared visual primitive/brand asset is defined in `components/ui/` + consumed from its barrel — ADR-0014 Rule 1/3) **AND a lint guard** `tests/unit/lint/reusable-visual-in-kit.test.ts` that fails CI if a brand/icon-style component is defined outside the kit (brace-aware detection catches all common icon shapes incl. block-body arrow, early-return, fragment-root, forwardRef/memo — hardened after Guard found the first version evadable). Guard NO BLOCKERS (tap + theme-lock verified; 1 🟡 guard-evasion FIXED on-branch). ANVIL right-sized CLEARED: unit/component 3082 · governance guard proven · full hosted @critical regression PASS on preview (CI smoke, first run); no migration/RLS/PITR/dep. Cert `docs/anvil/2026-06-30-haccp-hub-fixes-and-ds-governance-cert.md`. **Reusable lesson: brand assets + shared primitives are design-system citizens — define in the kit, never hand-wire into a screen; now lint-enforced.**
- **2026-06-30 (Phase 1 · `/haccp` hub — ✅ SHIPPED, PR #106 `e7ce730`)** — First Phase-1 screen done, full FORGE→ANVIL pass. Rebuilt `app/haccp/page.tsx` onto the kit (`components/ui/` + tokens); 3 new token-pure DEEP primitives (`StatusTile`, `ProgressRing`, additive `Banner.onClick`); pure logic extracted to `app/haccp/hubModel.ts`; login door migrated to the kit `PinKeypad` (F3, auth logic byte-preserved). One backend calc: `HaccpReportingService` `total_checks` 6→8 (honest daily mandatory set), response shape unchanged. All 5 deltas landed (help routing fix, drop "Online" dot, honest 8, surface mid-day operational-diary overdue [NOT into the alarm — pinned by test], "Goods In" label). Gate-2 design calls: F1 admin strip→header button, F2 keep `MfsLogo`+"Food Safety", F3 migrate PinKeypad, F4 include "Mandatory set · 8" checklist, F5 copy refresh; i18n stayed hardcoded EN (→ BACKLOG F-UI-I18N-01). Guard: NO BLOCKERS (alarm parity + login parity confirmed; rip-out PASS; no new port/adapter/dep); one 🟡 (tappable danger Banner missing `aria-live`) fixed on-branch `6da2acc`. ANVIL CLEARED: unit 3075/3075 · integration 4/4 new + 14/14 (live local Supabase, total=8 through the route) · exhaustive local @critical 12 new + 4 updated · **full hosted @critical suite PASS on the Vercel preview (CI smoke, first run, no flake)** · prod smoke non-5xx. No migration / no RLS / no PITR. **ADR-0014 first application** (Tier A → mockup-first). Cert `docs/anvil/2026-06-30-haccp-hub-ui-phase1-cert.md`; review `docs/reviews/2026-06-30-haccp-hub-ui-phase1-review.md`; design `docs/design/2026-06-30-haccp-hub.dc.html`; plan archived. **NEXT = `/haccp/cold-storage` (Tier B expected — standard temp-log form, build straight on the kit).**
- **2026-06-30 (Phase 1 · `/haccp` hub — requirements AUDITED, 5 deltas locked)** — Read-only audit (Explore agent) inventoried the live hub: a kiosk landing page = login door (staff card → PIN) + a 16-tile status board (Cold Storage · Process Room · Delivery · Mince/Prep · Product Return · Cleaning + 10 small tiles incl. admin-only Audit + Admin strip), every tile colour-coded live off `GET /api/haccp/today-status` (5-min poll), an escalating **audio alarm** on critical overdues, an overdue list (top strip on phone / side panel on iPad), and push-notification enable. All KEEP. **Five deltas locked with Hakan (each a deliberate decision, not a silent change):** **(#1 help text)** — 8 tiles currently all open the same "People" SOP by copy-paste bug → fix the bug so each shows its own existing text NOW; the real "map each tile → its HACCP policy-document section" is carved out as a **separate doc-audit workstream** (needs the actual HACCP policy doc(s); not blocking the hub). **(#2)** DROP the fake hardcoded "Online" dot (no real connectivity behind it; hub has no offline cache). **(#3 progress %)** make it HONEST — count the full set of fixed daily mandatory checks (cold AM/PM, room AM/PM, diary opening/operational/closing, cleaning), not just the current 6. **(#4)** SURFACE the computed-but-hidden mid-day "operational diary overdue" signal alongside opening/closing in the diary tile + overdue list. **(#5)** rename "Delivery" → **"Goods In"** — **display label only**; route `/haccp/delivery` + table `haccp_deliveries` stay (no ripple into the shipped goods-in label code). Must also honour decisions #19 (label↔UI boundary), #19a (collapsed-list dead-session error), #17 (no style-leaking props). **NEW: ADR-0014 ratified — "how screens consume the design system + tiered design workflow"** (Rule 1 code-always-composes-`components/ui/`+tokens; Rule 2 mock-up-first only for Tier-A novel layouts, Tier-B standard screens build straight; Rule 3 missing pattern → add to `components/ui/` first). Per-screen tier recorded in §5. **Hub = Tier A (novel kiosk layout) → visual mockup first.** Core-CCP log forms expected Tier B. **Next: write the hub brand prompt → Claude Design mockup → approve → FORGE build.**
- **2026-06-30 (🚀 UI PAUSE OVER — F-PROD-04 complete, Phase 1 HACCP-first STARTS)** — Label printing is **done**: **Pass 3 (real allergens on the DELIVERY label) SHIPPED** (PR #105 `13980c5`) — Frame corrected the brief (`allergens_identified` is a non-conformance flag, not a "contains" list) so it was delivery-label-only; new pure `formatDeliveryAllergens` helper + a `escapeHtml()` stored-XSS hardening Guard surfaced (free-text into the same-origin print iframe) and Hakan chose to fix in-PR; web-only, no migration/native; ANVIL unit 3036 · integration 9/9 · prod smoke 5/5 → 200. **Beef-labelling COMPLIANCE confirmed** (evidence-based audit vs gov.uk scheme + the inspector's email/code-list): compliant — veal dropped (don't sell), mixed-origin already handled (separate bookings = separate batches), country codes accepted (the inspector's attachment was the code list), prep fully compliant, mince needs no "Reared in" (only an optional "Origin:" wording nuance, left per Hakan); one operational rule logged (mince = single country of slaughter). **F-PROD-04 COMPLETE → decision #16 satisfied → UI un-paused.** **Architecture re-verified sound** (boundary pin-tests 120/120; only the sanctioned 9 supabaseService + 2 leaflet adapter imports; new helper pure/DEEP; rip-out test holds). **→ Phase 1 screen migration STARTS NOW — HACCP core CCP first** (§5 sequencing locked, §8 open item resolved). Phase 1 must honour decisions #19 (label↔UI boundary) + #19a (collapsed-list dead-session error) + #17 (no style-leaking props). Full label detail: BACKLOG §F-PROD-04.
- **2026-06-30 (UI still paused — beef mince + meat-prep DISPATCH labels FULLY DONE on-device)** — The
  label-printing job continues (UI paused per #16). RPA-email-driven beef dispatch labels shipped + confirmed
  on the V3: two BLS-compliant templates (MINCE country-only vs PREP country+plant + `Further cut in GB2946`),
  native on the die-cut roll, with three on-device follow-on fixes (mince 404 PR #103 · traceability chaining
  PR #104 · Born/Reared as country codes) and a native-layout calibration (vc7/1.6). **Confirms decision #19
  in practice again:** every change touched ONLY the server-side print route + the native bridge layout —
  zero design-system / Phase-1 surface. **Decisions #19/#19a still stand for Phase 1 to pick up.** Hakan chose
  **Pass 3 (real allergens)** as the next label step; then deferred beef items (veal/mixed-origin/processed +
  the full BLS regulatory review — chase the RPA inspector's attachment PDF); **then UI Phase 1 resumes,
  HACCP screens first** (the §8 "which screen first" question, answered). Full detail: `wc29` + BACKLOG
  §F-PROD-04 + memory `project_label_printing_next`.
- **2026-06-29 (UI still paused — F-PROD-04 Pass 2a SHIPPED)** — The label-printing job continues (UI paused per #16). **Pass 2 was SPLIT into 2a (Printer port — code) + 2b (release-signed APK — needs the physical Sunmi V3). Pass 2a (`Printer` transport port, refactor-only) SHIPPED** (PR #99 squash `9e765ac`): the two client-side print transports relocated behind one owned `Printer` port; both HACCP screens (`delivery`, `mince`) re-pointed to the wired printer → the last native-SDK-in-UI breach is closed. Byte-identical behaviour, 0 new deps, ADR-0010. **Confirms decision #19's boundary in practice:** the change touched ONLY the transport seam + the two screens' print wiring — zero design-system / Phase-1 surface; the server-side print HTML renderer was untouched. ANVIL CLEARED (unit 2973/2973 · @critical 78/78 preview · prod smoke 4/4 200). Decisions #19/#19a (label↔UI boundary + the deferred collapsed-list error-visibility fix) still stand for Phase 1 to pick up. **Pass 2b (release-signed APK) + Pass 3 (real allergens) remain before UI resumes.** Full detail: `wc29` + BACKLOG §F-PROD-04 + memory `project_label_printing_next`.
- **2026-06-29 (UI paused for label printing — F-PROD-04 Pass 1 SHIPPED)** — The post-Phase-0 pause (decision #16) is underway: repo/docs cleanup done (PR #97), now the label-printing job. **Pass 1 (print regression hardening) SHIPPED** (PR #98 squash `4a5d046`): the print path no longer silently prints the login page on a dead session — root cause was the T1 signed-cookie cutover bouncing the device's old cookie to `/login`, which the client printed; fix = shared `lib/printing/labelFetch.ts` refuses a login bounce. ANVIL CLEARED (unit 8/8 · @critical 78/78 preview · prod smoke 200). **Two UI-relevant decisions captured for Phase 1: #19** (label-printing ↔ UI boundary — printed labels are server-side print HTML, NOT the design system; DB-captured field = label-only change anytime, NEW-staff-entry field = app-screen via existing components for Phase 1 to restyle) and **#19a** (deferred collapsed-list dead-session error-visibility fix → Phase 1, needs per-row state). Pass 2 (Printer port + release-signed APK) + Pass 3 (real allergens) remain before UI resumes. Full detail: `wc29` + BACKLOG §F-PROD-04 + memory `project_label_printing_next`.
- **2026-06-29 (0b Wave 3 · Overlays + Nav — SHIPPED → PHASE 0 COMPLETE)** — **Wave 3 SHIPPED** (PR #96 squash `59a8f13`): 11 additive overlay+nav components in `components/ui/` (Modal [one Radix-Dialog engine, `center`+`sheet` variants], Banner, Spinner, EmptyState, Popover, DropdownMenu, AppHeader, BottomNav, MoreDrawer [composes Modal `sheet`], DesktopSidebar, NavItem) + dev-only `GalleryOverlaysNav` panel. Purely additive (23 new + 2 allowed edits: barrel + gallery wire), **no new dep** (radix-ui pre-existing/allow-listed; Next `<Link>` framework primitive), Tier-2 semantic tokens only. **Locked at Frame (decision #17 lens):** nav bricks are **presentational** (take `activeHref`/`expanded`/`sync`/`menu` as props — never read router/cookies/sync-queue; that logic stays in screens for Phase 1); **logo = caller `ReactNode` slot** (no baked asset path) → the **7 brand SVGs are a Phase-1 concern, NOT needed for 0b** (components are asset-agnostic; gallery uses placeholder). No style-leaking props. code-critic SHIP no-blockers (1🟡 MoreDrawer cmd/middle-click also closes sheet · 1🔵 inline iOS safe-area inset — both non-blocking, zero live impact pre-Phase-1); ANVIL right-sized CLEARED (component 81/81 Wave-3 · dir 219/219 · token guards 9/9 · tsc 0 · next lint clean · full unit 2947/2947 · NO E2E/integration/pgTAP/PITR = N/A presentation-only); CI `smoke` 75/75 first-run; setup.ts shim NOT needed (Radix Popover/DropdownMenu clean under jsdom). Cert `docs/anvil/2026-06-29-ui-phase-0b-wave3-overlays-nav-cert.md`; review `docs/reviews/2026-06-29-ui-phase-0b-wave3-overlays-nav-review.md`. **→ Phase 0b COMPLETE; Phase 0 done. Next: pause UI → repo/docs cleanup → label printing F-PROD-04 (decision #16).**
- **2026-06-29 (0b Wave 2 · Display — SHIPPED)** — **Wave 2 SHIPPED** (PR #95 squash `9b91885`): 11 additive display components + `accent.ts` helper in `components/ui/` (Card, CardHead, KpiTile, SectionLabel, PageHeading, ListRow, Table, SegmentedControl, Badge, StatusPill, SyncDot) + dev-only `GalleryDisplay` panel. Purely additive (24 new + 2 allowed edits: barrel + gallery wire), no new dep, no Radix (all plain semantic HTML), Tier-2 semantic tokens only. **3 API decisions locked at Frame on Hakan's "change once → every screen follows" architecture rule** (new memory `feedback_design_system_change_once`): (1) **Table = real semantic `<table>`** with dot-notation parts + `align`/`hideBelow` semantic props — NOT a grid faux-table with `widths` (accepted that Phase-1 admin re-point becomes a rewrite — buys the no-style-leak property); (2) **SegmentedControl only**, no speculative Radix Tabs (zero panel usage); (3) **three status components** (Badge + StatusPill + SyncDot), not one polymorphic Badge. Wave rule: **no style-leaking props** (no `className`/`widths`/inline-style) on any component. code-critic SHIP no-blockers (2 cosmetic 🔵 only); ANVIL right-sized CLEARED (component 59/59 Wave-2 · lane 138/138 · token guard 2/2 · tsc/build clean · NO E2E/integration/pgTAP/PITR = N/A presentation-only); CI `smoke` 75/75 fresh; prod non-5xx. Cert `docs/anvil/2026-06-29-ui-phase-0b-wave2-display-cert.md`; review `docs/reviews/2026-06-29-ui-phase-0b-wave2-display-review.md`. **Next: W3 Overlays+Nav** (last wave; pull brand SVGs before it).
- **2026-06-29 (0b Wave 1 · Forms — SHIPPED)** — 0b split into **3 FORGE→ANVIL→merge waves** at Frame: W1 Forms · W2 Display · W3 Overlays+Nav (refines decision #13's "0b = one PR" — Hakan chose waves to fit never-batch; nav last so brand SVGs are pulled just before W3). **Wave 1 SHIPPED** (PR #94 squash `ff7a005`): NEW additive `components/ui/` library — 11 form components (Button, IconButton, TextField, Textarea, Select, Checkbox, Radio, Toggle, FormField, PinKeypad, Picker) + barrel + dev-only `/dev/ui` gallery. Additive, zero existing files touched, no new dep. Locked at Gate 2: ARIA labels = optional props w/ English defaults (Phase-1 passes `t()`); icons = caller `ReactNode` (no icon lib). code-critic SHIP no-blockers; ANVIL CLEARED (component 79/79 · suite 2807/2807 · token guards 13/13 · tsc/lint/build clean · 0 bugs); CI `smoke` 75/75; prod non-5xx. Gallery reviewed via local screenshots — it 404s on deploys + middleware blocks it → backlog **F-UI-GALLERY-01** (make preview-reachable). Cert `docs/anvil/2026-06-29-ui-phase-0b-wave1-forms-cert.md`; review `docs/reviews/2026-06-29-ui-phase-0b-wave1-forms-review.md`. **Next: W2 Display.**
- **2026-06-29 (worktree retired → main dir)** — UI rebuild moved out of the worktree into the **main working directory**. Removed `.claude/worktrees/ui-system-rebuild` + force-deleted its squash-merged branch `feat/ui-phase-0a-foundation` (clean; 0a code already in `main` via PR #93). The worktree only existed to isolate UI work from the parallel 16-day sprint, which is now sealed — so it's no longer needed. Section 1 guardrails updated: merge is unlocked, work happens in the main dir, each section still ships via feature branch → PR → `smoke`. Stale local branches (`worktree-ui-system-rebuild`, `feat/ui-overhaul-01/02/03`, old `f-td-*`/`anvil-*`) left for the step-5 cleanup sweep. Next: **0b**.
- **2026-06-28 (0a SHIPPED → main)** — Terminal-1 closed the 0a cert and merged. Cert flipped PENDING→CLEARED on a clean-DB `@critical` **75/75** run (production build, fresh `db:reset`). The run took 3 attempts; BOTH early fails were worktree test-harness wiring, NOT the diff: (1) worktree lacked `.env.e2e.local` PINs (gitignored files don't enter a worktree) → 66 login-throws; (2) the hand-rolled prod server lacked `SUPABASE_JWT_SECRET` so every authenticated WRITE 500'd (`cannot mint DB identity tokens`, the F-RLS-04i ForCaller path) → 65 write-fails. Fixed both → 75/75. Merged via **PR #93** (squash `6a847fa`); `smoke` required check green 75/75; CLEAN merge (no override, no F-INFRA-07 caveat). **Prod redeployed → reskin LIVE** (`mfs-operations.vercel.app` serves new orange `#EB6619` + Inter). Decisions #14 (stepwise merge), #15 (reskin accepted), #16 (pause-after-0b for printing) locked. Next: **0b**.
- **2026-06-28 (Guard+ANVIL)** — 0a Guard + ANVIL. **code-critic: NO BLOCKERS** (re-ran suite 2710✓, hand-verified R2 build-safety — every legacy colour utility resolves, no screen goes colourless; R1 alpha channel-forms correct; tokens.css verbatim vs reference; radix allow-listed/zero-imports; scope clean). Review at `docs/reviews/2026-06-28-ui-phase-0a-foundation-review.md`. **ANVIL:** unit 2710✓ · component 4✓ · guards 12✓ · tsc clean · `next build` green · **visual smoke (PROD build, 6 screens): new tokens LIVE — orange #EB6619, body bg #EDEAE1, Adieu+Inter, Plus Jakarta retired, maps mount clean, 0 console errors** · integration/pgTAP/RLS/PITR = justified N/A (no migration/API/policy). **E2E @critical 62/75 — 13 fails ALL environmental** (dirty SHARED local DB from a prior F-20 run + a dev-server-only Leaflet double-mount that's clean under prod build), NONE from this diff (failing DOM carries the new tokens). **Decision: DEFER the clean-DB @critical to merge-time** (don't `db:reset` the shared DB) — cert PENDING (`docs/anvil/2026-06-28-ui-phase-0a-foundation-cert.md`). Terminal 1 (main seal) is DONE → DB now free → that rung is a quick pre-merge step. **Non-blocking follow-up:** make `token-resolve.test.ts` LEGACY_COLOR_NAMES derive from a live content-grep (currently hand-maintained) — 0b hardening.
- **2026-06-28 (Render)** — 0a BUILT on `feat/ui-phase-0a-foundation` (9 atomic commits `b43142a`→`ca8a4ea`, unpushed). New `app/tokens.css` (verbatim from the reference), `globals.css` + `tailwind.config.ts` rewired to read the vars (double-declaration killed), Adieu+Inter via `next/font` (Plus Jakarta retired), vitest split node/jsdom lanes, semantic-tokens lint + token-resolve guards, `radix-ui` allow-listed. Suite green: **2714 pass / 0 fail**; `tsc` clean; `next build` green. Conductor calls: deleted 2 superseded pre-rebuild oracles (`tests/unit/design-system/*`); ratified +4 legacy colour aliases, `vitest-axe.d.ts`, and the forced `@testing-library/dom` RTL peer. **DEFERRED: the 7 brand SVGs** (`public/brand/logo-*.svg`, `star-icon-*.svg`) — DesignSync unavailable in subagent context + hand-transcription risks corrupting vector artwork; non-load-bearing for 0a (no test/build references them). TODO before any screen needs a logo: place via a safe sync (conductor has DesignSync at session level). Next: Guard (code-critic) → ANVIL → ship-when-unlocked.
- **2026-06-28 (later)** — FORGE Phase 0 kicked off. Frame/grill locked the spec; **Phase 0 split into 3 PRs** (0a/0b/0c — decision #13). **ADR-0009** written (Radix + component-test stack). Design `.dc.html` pulled fresh to disk (`docs/design/MFS-Operations-Design-System.dc.html`; last ~8KB truncated at the 256KiB fetch cap — 0b gallery only, foundation intact) + verbatim token reference saved (`docs/design/phase0a-foundation-tokens.reference.css`). **Inertness REVERSED → fidelity** (decision #12): foundation implements the design tokens verbatim, old colours/Plus-Jakarta retired, existing screens shift look immediately (safe on isolated branch). 0a plan (re)written at `docs/plans/archive/2026-06-28-ui-phase-0a-foundation.md`. Next: Gate 2 plan approval → Render.
- **2026-06-28** — Design delivered by Claude Design, reviewed STRONG/build-ready; both open items resolved (Adieu font licensed; spacing = density system). Density default = comfortable confirmed. Roadmap created. Next: FORGE Phase 0.
- **2026-06-27** — ui-cartographer wrote `docs/ui-current-state.md`; brand-prompt fork wrote the Claude Design brief; isolated work into the `worktree-ui-system-rebuild` worktree after discovering the shared-folder branch collision; locked decisions 1–8; saved per-section discipline to memory.
