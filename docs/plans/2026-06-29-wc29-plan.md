# wc29 — Week Commencing 29 June 2026 (execution plan)

> **Short name: `wc29`.** This is the weekly execution tracker. Companion: the UI rebuild
> single-source-of-truth is `docs/plans/2026-06-28-ui-overhaul-roadmap.md` (short name **`UI roadmap`**).
> Update the Status + Change log here each working session.

**Owner:** Hakan · **Driver:** this chat (Terminal 1, taking over the UI rebuild from the closed worktree terminal).
**Working location:** the **main working directory** (`/Users/hakankilic/MFS-Operations`). The old UI worktree was retired 2026-06-29 — see change log.

---

## Locked decisions for the week (2026-06-28)

| # | Decision | Note |
|---|----------|------|
| A | **Integration model = merge STEP BY STEP** (not accumulate-then-merge-at-end) | reverses the roadmap's original "merge the whole overhaul at the end" intent — Hakan's call |
| B | **Reskin accepted (option A):** merging 0a/0b to `main` re-skins every existing un-migrated screen with the new palette/fonts immediately. Accepted as harmless on an internal staff tool. | consequence of decision #12 (fidelity-not-inertness) meeting stepwise merge |
| C | **Pause UI after Phase 0** (0a + 0b), BEFORE any Phase-1 screen, to finalise **label printing** (F-PROD-04 — critical) | no screen gets rebuilt before the printing job |
| D | **0c is NOT a discrete step** — it's deferred/build-on-demand by design (roadmap #13); speculative components get built when a Phase-1 screen needs one. After 0b, Phase 0 is effectively complete. | |

🗣 Plain English: we hand the new building over one finished room at a time (A); turning on the
new paint supply recolours the old rooms too and we've decided that's fine for an internal tool (B);
once the foundation + the parts cupboard are in, we down tools on the redesign and go fix the till,
because the till is what makes money (C); there's no separate "0c job" to schedule — those extra
parts get made only when a specific screen calls for them (D).

---

## The sequence (this week + into the printing job)

```
wc29
  1 ✓ close 0a cert (db:reset → @critical 75/75 on prod build → CLEARED)        DONE
  2 ✓ merge  0a → main (PR #93 squash 6a847fa; smoke 75/75; prod reskin LIVE)   DONE
  3 ✓ build  0b core component library — split into 3 waves, each its own FORGE→ANVIL→merge:
              W1 Forms ✓ SHIPPED (PR #94 ff7a005) · W2 Display ✓ SHIPPED (PR #95 9b91885) · W3 Overlays+Nav ✓ SHIPPED (PR #96 59a8f13)  DONE
  4 ✓ merge  0b COMPLETE — all 3 waves in main → Phase 0 (0a+0b) effectively complete    DONE
  5 ✓ tidy   repo/docs cleanup on clean main — docs reorg ✓ SHIPPED (PR #97 b1e14e2) + ALL stale branches swept (8 local + 18 remote, every one verified merged-or-contained-in-main) → ONLY main remains, local AND remote — see project_repo_cleanup_deferred  DONE
  6 ✓ PAUSE UI → finalise label printing (F-PROD-04, critical)  ── the pivot   DONE
  7 ○ RESUME UI → Phase 1 screen migration, HACCP section FIRST   ── next
```

> **Tracker map (Hakan, 2026-06-30): TWO living trackers + one stable reference.**
> ① **UI roadmap** (`2026-06-28-ui-overhaul-roadmap.md`) = the **master UI tracker** (Phase 0/1, decisions, screens).
> ② **wc29** (this file) = the **weekly execution log**.
> Label-printing detail lives ONCE in **BACKLOG §F-PROD-04** — the trackers point to it, never duplicate it.
> `2026-06-27-ui-system-rebuild-process.md` = stable per-section process contract (reference, not updated each session).

## Status

| Step | What | Status |
|------|------|--------|
| 1 | Close 0a ANVIL cert — `db:reset` → prod build → `@critical` **75/75** → cert CLEARED | ✅ done |
| 2 | Merge 0a to `main` — PR #93 squash `6a847fa`, smoke 75/75, **prod reskin LIVE** (`mfs-operations.vercel.app` serves new orange `#EB6619` + Inter) | ✅ done |
| 3 | Build 0b core library — **3 waves**: W1 Forms ✅ SHIPPED (PR #94 `ff7a005`) · W2 Display ✅ SHIPPED (PR #95 `9b91885`) · W3 Overlays+Nav ✅ SHIPPED (PR #96 `59a8f13`) | ✅ done |
| 4 | Merge 0b to `main` — **0b COMPLETE**, all 3 waves in; Phase 0 (0a+0b) effectively complete | ✅ done |
| 5 | Repo/docs cleanup (clean main) — docs reorg ✅ SHIPPED (PR #97 `b1e14e2`: README index + `reference/` tree + archived stragglers; cert `docs/anvil/2026-06-29-docs-structure-cleanup-cert.md`) + **ALL 8 stale local branches swept** (6 verified-merged + the 2 no-PR ones confirmed fully superseded by inspection — `worktree-ui-system-rebuild` docs all in main, `test/anvil-chrome-matrix` spec a stale pre-rename draft of main's current one). Plus **18 stale REMOTE branches pruned** (8 PR-merged + 10 no-PR but fully contained in `main` as ancestors — nothing unique). **Only `main` remains, local AND remote — repo fully clean.** | ✅ done |
| 7 | **RESUME UI → Phase 1 screen migration, HACCP FIRST** — label printing complete, UI un-paused (decision #16 satisfied). First section = HACCP core CCP (`/haccp`, `/haccp/{cold-storage,process-room,delivery,mince}`) per UI-roadmap §5; per-section requirements-audit-FIRST gate (decision #8/#17), must pick up decisions #19 (label↔UI boundary) + #19a (collapsed-list dead-session error). | ○ NEXT — Hakan starts this |
| 6 | Label printing F-PROD-04 (pause UI) — ✅ **COMPLETE 2026-06-30 (all passes shipped + beef-labelling compliance confirmed).** Hakan scoped "fix completely" into **sequenced FORGE passes**. **Pass 1 (regression hardening) ✅ SHIPPED 2026-06-29 (PR #98 `4a5d046`).** Root cause = APK is a remote-URL shell loading the live site + T1 signed-cookie cutover bounced the device's pre-T1 cookie to `/login`, which the print client silently printed. Fix = shared `lib/printing/labelFetch.ts` refuses to print a login bounce + shows a re-login message. ANVIL CLEARED: unit 8/8 · @critical 78/78 preview · prod smoke 200. Cert `docs/anvil/2026-06-29-fprod04-pass1-print-regression-cert.md`. **Pass 2 SPLIT into 2a (Printer port — code) + 2b (release-signed APK — needs the physical V3).** **Pass 2a (`Printer` transport port, refactor-only) ✅ SHIPPED 2026-06-29 (PR #99 `9e765ac`):** the two client-side transports relocated behind one owned port (renderer left port-less by design; Zebra = unbuilt future slot); both HACCP screens re-pointed to the wired printer → native-SDK-in-UI breach closed; byte-identical, 0 new deps, ADR-0010. ANVIL CLEARED (unit 2973/2973 · @critical 78/78 preview · prod smoke 4/4 200). **Pass 2b (release-signed APK + MFS launcher icon) ✅ SHIPPED 2026-06-30 (PR #100 `40f446d`):** fresh keystore (Hakan, gitignored) + conditional signingConfig + versionCode 2 + web-debug off in release + buildConfig=true (AGP 8.13); MFS icon (full-bleed adaptive, V3 is API 33); signed APK verified on the physical V3 (install + login + print + icon). Accepted risk: signing pw leaked in transcript, kept. **die-cut label sizing (DELIVERY) ✅ SHIPPED 2026-06-30 (PR #101 `84e886a` + on-device calibration `816455c`):** receipt→label mode @ 52×38mm; version-tolerant JSON bridge (ADR-0013, fixed a silent APK↔web desync); confirmed fitting on the V3 via `printColumnsString` + device label-learning. **beef mince + meat-prep BLS DISPATCH labels ✅ FULLY DONE 2026-06-30 — BOTH labels confirmed printing + fitting on the V3.** Core ship PR #102 `0a3a1cb` (RPA-email-driven; MINCE country-only vs PREP country+plant + `Further cut in GB2946`, `MFS_PLANT_CODE` const replaced UK2946, server-aggregates-once + native `format=json`, bridge `mince`/`prep` branches no-signature-change ADR-0013; Guard NO BLOCKERS; ANVIL CLEARED unit 3016 · @critical 78/78 after an F-INFRA-07 reseed · prod smoke 200). **Native calibrated vc7/1.6 `d2d0ce6`** (dropped bold batch line → batch in barcode caption + removed under-barcode gap → fits 52×38). **THREE on-device follow-on fixes:** (1) mince 404 (PR #103 `5ed1c24`) — `/api/labels` mince select asked for `allergens_present` (column only on `haccp_meatprep_log`) → PostgREST 404 → native fell back to iframe (can't print on V3); dropped it, render None till Pass 3; (2) traceability chaining (PR #104 `c78d5eb`) — prep/mince were BLANK for Born/Slaughtered/Cut when sourced from another BATCH (burger from a mince batch, empty `source_delivery_ids`); new `resolveDeliveryIds()` follows source batches back to their deliveries (MINCE-/PREP- recurse, depth-bounded); (3) Born/Reared as country CODES (GB,AU) for fit. All verified vs prod DB + @critical 78/78 (PR #104's 1st smoke failed at the cred-sync readiness gate in 50s — preview not wired, NOT code — reran SAME head → green). **NEXT = Pass 3 (real allergens on delivery + mince/prep — stop hardcoding None).** Open gaps: vitest integration in CI (would've caught the 404; not in `smoke`) · native `printLabel` catch swallows exceptions → render errors fail SILENTLY · add a prep-from-mince-batch chain integration case. **Pass 3 (real allergens on the DELIVERY label) ✅ SHIPPED 2026-06-30 (PR #105 `13980c5`)** — Frame corrected the brief (`allergens_identified` is a BOOLEAN non-conformance flag, not a "contains" list) → delivery label only (None / red notes / FLAGGED); prep already correct, mince genuinely None; new pure `formatDeliveryAllergens` helper + `escapeHtml()` XSS hardening on all delivery-HTML free-text; web-only, no migration/native; ANVIL unit 3036 · integration 9/9 · prod smoke 5/5 → 200. **Beef-labelling COMPLIANCE confirmed 2026-06-30** (gov.uk scheme + inspector email/code list, evidence-based audit): compliant — veal N/A (don't sell), mixed-origin already handled (book separately = separate batches), country CODES accepted (inspector provided the list), prep label fully compliant; mince needs no "Reared in" (only an "Origin:" wording nuance, left per Hakan); one operational rule noted (mince = single country of slaughter). **→ F-PROD-04 COMPLETE.** Full detail: BACKLOG §F-PROD-04 + memory `project_label_printing_next`. | ✅ **COMPLETE — all passes shipped + compliant; UI un-paused → Phase 1 HACCP next** |

## Step-5 cleanup — scope + the HACCP-handbook architecture watch

**Cleanup scope (2026-06-29):** docs/file-structure tidy ONLY — no code moved, no import paths
changed. Reorg plan: new `docs/README.md` index + new `docs/reference/{haccp,routing,printing,security,architecture}/`
home for evergreen docs; shipped/stale plans → `docs/plans/archive/`; stale things ARCHIVED, never
deleted. Pinned files stay put (`BACKLOG.md`, `adr/**`, `runbooks/preview-smoke.md`, the live UI trio).
App-impact = **zero, grep-proven**: the only non-`docs/` references to any moved file are 3 developer
COMMENTS (`app/haccp/training/page.tsx`→DOCUMENT_CONTROL.md ×3, `app/api/routes/optimise/route.ts:21`→routing-engine-spec.md,
and one frozen migration comment→rls-audit). The app never reads these `.md` at runtime. The 2 in-app
comments get repointed in the same commit as the move; the migration comment is left as a frozen
breadcrumb (never edit an applied migration).

**⚠ ARCHITECTURE WATCH — the butcher-facing HACCP handbook is a LIVE hexagon, NOT a doc being moved.**
Do NOT confuse the dev/compliance docs being filed (`HACCP_AUDIT_STATUS.md`, `HACCP_CCP1_AUDIT.md`,
the dev-side `DOCUMENT_CONTROL.md`, `HACCP_ALARMS_PLAN.md`) with the handbook butchers actually read.
That content is database-backed and surfaced in the app:

```
HACCP Handbook (what butchers read) — owned port/adapter hexagon, lives in the APP
  DATABASE (Supabase)
    ├─ haccp_sop_content  → SOP handbook text (markdown in content_md)
    ├─ haccp_documents    → Document Control Register (doc_ref/version/owner/review_due)
    └─ haccp_search (RPC) → full-text search over the handbook
  ▲ read via owned port  lib/ports/HaccpHandbookRepository.ts
  ▲ Supabase adapter     lib/adapters/supabase/HaccpHandbookRepository.ts  (+ Fake for tests)
  ▲ service+wiring       lib/services/HaccpHandbookService.ts · lib/wiring/haccp.ts
  ▲ API                  app/api/haccp/{handbook,search,documents}/route.ts
  ▲ butcher screens      /haccp/documents · /haccp/documents/[ref]   (21 /haccp/* screens total)
  seeded by              supabase/migrations/20260101000000_baseline.sql
🗣 the handbook content is in the database; butchers read it on the in-app screens — the docs/
   cleanup cannot and does not touch it
```

**TO REVIEW (tracked, per ports-and-adapters):** give the HACCP-handbook hexagon a proper architecture
look — confirm it satisfies the rip-out test (one adapter + one wiring line), how handbook/document
content is authored & version-controlled in the DB, and whether the dev-side `DOCUMENT_CONTROL.md`
register should reconcile with the live `haccp_documents` table. Scope this with Hakan before the
label-printing pivot. (Mirror this as a BACKLOG entry if it grows into real work.)

## Guardrails carried from the UI roadmap (do not break)

- Presentation layer ONLY (`app/**`, `components/**`, token/tailwind/test config). NEVER touch
  `lib/ports|adapters|wiring/**` (import existing singletons only), `middleware.ts`, auth/RLS,
  `app/api/**` route auth, `.github/**`, Vercel settings. New service/port/adapter ⇒ STOP, ask Hakan.
- No new vendor SDK in the UI except already-approved `radix-ui`. Test-only devDeps exempt.
- No AI references in commits/PRs/code/comments.
- Merge while ON the feature branch so `anvil-migration-lock.sh` matches the cert's bare `Branch:` line.

## Ops notes (reusable)

- **(HISTORICAL — worktree retired 2026-06-29; we now work in the main dir, which already holds all
  `.env.*.local`. This note is kept only for if a worktree is ever reintroduced.)** A git worktree does NOT contain gitignored files. `.env.e2e.local` (E2E PINs), `.env.test.local`
  (local Supabase wiring), and `.env.local` (prod) live only in the MAIN dir. Running `@critical` from the
  worktree without them → every login throws `Missing E2E_PIN_* env var` → 60+ cascade fails that look
  catastrophic but are pure wiring. **Fix:** copy `.env.e2e.local` + `.env.test.local` into the worktree
  (NOT `.env.local` — keep prod values out so `next start` can't load them). Reading `.env*` via Bash is
  sandbox-blocked; the copy needs `dangerouslyDisableSandbox` (local file copy, no external effect).
- **Prod-build E2E recipe (clears the dev-only Leaflet map fails):** `npm run build` (NEXT_PUBLIC_* are
  inlined at build → must be local; shell exports from `supabase status -o env` beat `.env.local`) →
  start the prod server with the **COMPLETE** `.env.test.local` sourced (`set -a; source ./.env.test.local;
  set +a; npm run start` on :3000) → `npx playwright test --project=chromium --grep @critical` (Playwright
  `reuseExistingServer` reuses the prod server instead of booting dev).
- **CRITICAL: a hand-rolled prod server needs the FULL `.env.test.local`, not a hand-picked subset.** The
  4 obvious vars (URL/anon/service-role/session) are NOT enough — the per-caller RLS write path
  (F-RLS-04i `…ForCaller`) mints a DB-identity token signed with **`SUPABASE_JWT_SECRET`** on every
  authenticated WRITE; without it every write 500s (`SUPABASE_JWT_SECRET is not set — cannot mint DB
  identity tokens`) → reads pass, writes cascade-fail (looks like a regression; it's wiring). The dev
  server never hits this because Playwright loads all of `.env.test.local` into its own process env and
  the spawned dev server inherits it. Symptom signature: ~10 pass (reads), ~65 fail (writes), order specs
  stuck waiting on the `MFS-####-####` reference that never appears.

## Change log

- **2026-06-30 (step 6 → step 7 · F-PROD-04 Pass 3 SHIPPED + beef-labelling COMPLIANCE confirmed → label printing COMPLETE, UI un-paused)** — **Pass 3 (real allergens on the goods-in DELIVERY label) SHIPPED** via gated FORGE→ANVIL (PR #105 squash `13980c5`, docs follow-up `a2ebe69`). **Frame corrected the brief:** `haccp_deliveries.allergens_identified` is a BOOLEAN non-conformance WARNING flag (MFS is allergen-free; true = a problem found at intake), NOT a product "contains" list → Pass 3 = DELIVERY label ONLY (not-flagged "Allergens: None" byte-identical / flagged → `allergen_notes` in red / flagged+blank → "FLAGGED - see record"). PREP already correct (reads `allergens_present`); MINCE genuinely "None" (no allergen data model; pure beef = allergen-free). New pure helper `lib/printing/allergens.ts` `formatDeliveryAllergens` = single source for all 4 transports. **Guard found + Hakan chose to fix-now a stored-XSS gap** — DB free-text was interpolated raw into the same-origin print iframe (pre-existing class) → added `escapeHtml()` to ALL delivery-HTML free-text fields (commit `2a4becc`). Web-only, NO migration/RLS/native/dep. ANVIL CLEARED (`docs/anvil/2026-06-30-fprod04-pass3-delivery-allergens-cert.md`): unit 3036 (R1 byte-identical exact-string oracles on all 4 transports + 4 XSS specs) · integration 9/9 LIVE (Docker back up) · @critical 78 CI (F-INFRA-07 dirty-DB flake on the 3rd push → reset preview branch `b1e474fe…` + rerun SAME head → clean) · prod smoke 5/5 → 200. Review `docs/reviews/2026-06-30-fprod04-pass3-delivery-allergens-review.md`. NEW 🔵 follow-up: barcode-SVG `batch_code` still raw (escape later; 🔴 only if batch_number becomes user-editable). **Then BEEF-LABELLING COMPLIANCE confirmed** (evidence-based audit vs gov.uk "Comply with the Compulsory Beef Labelling Scheme" + the inspector's email + his country-code attachment): **compliant.** Veal ❌ DROPPED (MFS doesn't sell veal → N/A). Mixed-origin ✅ NOT a gap (Hakan right — the intake form is one-origin-per-submission + the server mints a fresh batch per row, so booking a mixed consignment as separate deliveries = separate batches, exactly the RPA "book separately" rule; training, not a build). Country CODES (GB/AU) ✅ accepted (the inspector's attachment WAS the shortened code list). PREP label ✅ fully compliant (Born in/Reared in/Slaughtered in site/Cut in site/Further cut in GB2946). MINCE ✅ needs NO "Reared in" (scheme conveys origin via an "Origin:" line, not separate born/reared) — only a one-word "Born in:"→"Origin:" nuance, **left untouched per Hakan**. 🆕 one OPERATIONAL rule logged: a mince batch must be from a SINGLE country of slaughter (system doesn't warn — likely non-issue). **→ F-PROD-04 COMPLETE. Architecture re-verified sound (boundary pin-tests 120/120 green; only the sanctioned 9 supabaseService + 2 leaflet adapter imports; new helper pure/DEEP). UI un-paused (decision #16 satisfied) → step 7 = Phase 1 screen migration, HACCP section FIRST.** Full detail: BACKLOG §F-PROD-04.
- **2026-06-30 (step 6 · BEEF mince + meat-prep DISPATCH labels — FULLY DONE on-device)** — The next F-PROD-04
  unit, driven by the RPA inspector's beef-labelling email (Daniel Hamilton, RPA). Gated FORGE→ANVIL pass
  (PR #102 `0a3a1cb`): two BLS-compliant dispatch templates printing natively on the V3 die-cut —
  **MINCE** country-only (`Slaughtered in GB` / `Minced in GB`, no plant code) vs **PREP** country+plant
  (`Slaughtered in GB1234` / `Cut in GB5678` / `Further cut in GB2946`); `MFS_PLANT_CODE='GB2946'` const
  replaced the hardcoded UK2946 (GB is the BLS prefix, not the UK health-mark form); server aggregates the
  BLS data ONCE, native consumes via `format=json` (single source of truth); native bridge gained
  `mince`/`prep` `type`-branches with NO signature change (ADR-0013). 5 decisions locked via AskUserQuestion
  (GB2946 · native V3 die-cut · beef-correct-now-defer-rest · multi-source=distinct-comma-join ·
  mince→MINCE/meatprep→PREP). code-critic NO BLOCKERS; ANVIL CLEARED (unit 3016/3016 · @critical 78/78,
  F-INFRA-07 dirty-DB flake on the test-only 2nd push → MCP reset_branch + rerun SAME head → clean · NO
  migration/RLS/PITR/dep). **Native calibrated on-device (vc7/1.6 `d2d0ce6`):** dropped the bold batch line
  (batch stays in the barcode HRI caption) + removed the under-barcode gap → both labels fit 52×38.
  **THREE on-device follow-on web fixes** (each PR + smoke, server-side so no APK reinstall): (1) **mince 404**
  PR #103 `5ed1c24` — mince select asked `haccp_mince_log` for `allergens_present`, a column only on
  `haccp_meatprep_log` → PostgREST 404 → native fell back to the iframe (can't print on V3) → "Could not
  print label"; PRE-EXISTING latent bug surfaced by the native path; dropped the column, render None till
  Pass 3 (diagnosed via Vercel prod logs = 404 not 401). (2) **traceability chaining** PR #104 `c78d5eb` —
  prep/mince printed BLANK Born/Slaughtered/Cut when a run was sourced from another BATCH (the burger
  PREP-3006-IMPVAC-1 sourced MINCE-3006-BEEF-1 with empty `source_delivery_ids`); new `resolveDeliveryIds()`
  follows source batches back to their deliveries (MINCE-/PREP- → their source rows, PREP recurses 1 level
  depth-bounded; else a goods-in `batch_number`). (3) **Born/Reared as country CODES** (GB, AU) for fit +
  consistency (goods-in label unchanged). All verified vs the live prod DB. **Reusable ops:** when blind to
  the device's JS console (release build hides it), pull the Vercel runtime logs — the V3 fetches label data
  from prod, so the server logs the real status code (404 here, decisive). On-device bugs are often the
  WEB/route, not the native bridge (remote-URL shell). Smoke can also fail at the cred-sync READINESS GATE
  (~50s, preview not wired yet) — distinct from the F-INFRA-07 dirty-DB flake; both clear by rerunning the
  SAME head once the preview branch is ACTIVE_HEALTHY. Native calibration = publish-then-calibrate: edit the
  Java layout → `gradlew assembleRelease` → `adb install -r` → reprint (versionCode bumped 5→6→7); committed
  straight to main (native-only, admin-override — web smoke can't cover thermal print). **→ NEXT = Pass 3
  (real allergens on delivery + mince/prep — stop hardcoding "None"), Hakan's pick.** Then deferred beef
  items + UI Phase 1 (HACCP first).
- **2026-06-29 (step 6 · label printing — Pass 2a SHIPPED; Pass 2 split into 2a/2b)** — Hakan said "start
  Pass 2"; at Frame we SPLIT it into **2a (Printer port — code, CI-testable)** and **2b (release-signed APK —
  build artifact, needs the physical Sunmi V3)** because they have different verification paths. **Pass 2a
  (`Printer` transport port, refactor-only) SHIPPED — PR #99 squash `9e765ac`.** Grill corrected the brief:
  only 2 LIVE transports (Sunmi native + iframe/AirPrint), NOT 3 — Zebra-ZPL is a dormant render format, left
  a named-but-unbuilt slot; the server-side renderer (`generateLabel`/`html`/`zpl`/`types`) deliberately left
  PORT-LESS (pure, no vendor; wrapping fails the deletion test). Two transport files relocated VERBATIM
  (git-rename): `lib/printing/sunmi.ts` → `lib/adapters/sunmi/Printer.ts`, `lib/printing/labelFetch.ts` →
  `lib/adapters/browser/Printer.ts`; new `lib/ports/Printer.ts` + contract + Fake; client wiring
  `lib/wiring/printer.ts` `getPrinter()` selects by device AT CALL TIME (SSR-safe, mirrors F-26
  Dexie/LocalCache), Browser injected as Sunmi's fallback. Both HACCP screens re-pointed to the wired printer
  only → **native-SDK-in-UI breach CLOSED**. Byte-identical behaviour, 0 new deps, NO DB. ADR-0010 (Accepted).
  code-critic SHIP no-blockers (new modules DEEP, rip-out PASS). ANVIL CLEARED
  (`docs/anvil/2026-06-29-fprod04-pass2a-printer-transport-port-cert.md`): unit **2973/2973** (R1 mince
  `usebydays` URL byte-identity 11/11 — food-safety · R2 native-jam→iframe fallback 6/6) · tsc + `next lint`
  clean · @critical **78/78** preview (CI smoke `28400850484`) · focused browser-tap of both print screens ·
  prod smoke 4/4 200. **NOT proven here (deferred to 2b):** the PHYSICAL V3 native print — code MOVED not
  changed; selection→fallback unit-tested with a faked bridge (correct 2a proof). **Ops:** `next lint`
  sandbox-denied for the anvil-runner → conductor ran it at Lock (established pattern); the planner-written
  plan was never git-tracked (forge-planner writes to `docs/plans/`, not committed) → archived via plain `mv`;
  repo swept back to only-`main`. **Next: Pass 2b (release-signed APK, needs the device).**
- **2026-06-29 (step 6 · label printing — Pass 1 SHIPPED + 3-pass plan locked)** — Hakan scoped "fix label
  printing completely" into **3 sequenced FORGE passes**. **Diagnosis (3 parallel read-only traces):** the
  Sunmi V3 APK is a thin Capacitor **remote-URL shell** (`capacitor.config.ts:8` → `server.url:
  https://mfsops.com`) loading the LIVE site, so "worked then broke ~1wk later" = the website changed under
  the unchanged native app, NOT the printer bridge. Root cause = the T1 signed-cookie cutover (`88af11d`,
  2026-06-12) made `middleware.ts:121-134` HMAC-verify `mfs_session` and FAIL CLOSED on legacy cookies → the
  device's pre-T1 cookie bounced to `/login` (200 HTML) → the print client checked only `res.ok` and silently
  printed the login page. **Pass 1 (regression hardening) SHIPPED — PR #98 squash `4a5d046`:** shared
  `lib/printing/labelFetch.ts` (`classifyLabelResponse` detects the `/login` bounce by pathname) refuses to
  print + shows "Session expired — log in again" via existing `submitErr`; dedupes the delivery + mince copies.
  Client-only, no migration/auth/bridge change, happy path byte-identical. code-critic NO BLOCKERS (helper
  DEEP, hexagonal N/A — no vendor); ANVIL CLEARED: unit 8/8 · @critical **78/78** clean preview · prod smoke
  all 200 (cert `docs/anvil/2026-06-29-fprod04-pass1-print-regression-cert.md`, review `docs/reviews/2026-06-29-fprod04-pass1-print-regression-hardening-review.md`).
  **Reusable ops:** (1) NO device needed (remote shell ⇒ browser/iframe path proves the device path); band-aid
  for the live break = **log out + back in on the device** (fresh signed cookie). (2) new spec found 2 TEST bugs
  on first preview run — main-frame `window.print()` spy can't see a print fired in the iframe's `contentWindow`
  (assert the print iframe is created instead); shared `submitErr` mounts in multiple tab sections → visibility
  checks need `.first()` (strict mode). (3) F-INFRA-07 dirty-preview-DB flake recurred → MCP `reset_branch` on
  preview branch `373ca93f…` → ACTIVE_HEALTHY → `gh run rerun` SAME head → clean 78/78. (4) anvil-runner
  SUSPENDED (local Docker down) → CONDUCTOR finished E2E via CI per standing rule. (5) cert hook needs a BARE
  `Branch:` line (no `**bold**`) AND the literal phrase `CLEARED FOR PRODUCTION`. **Decisions locked:** Pass 2 =
  `Printer` port (last no-port module) **+ build a release-signed APK** (current install is a fragile DEBUG
  build) — keep manual USB sideload AND publish to Google Play if easy (account exists); Pass 3 = real allergens
  on the delivery label (`html.ts:211` hardcodes "None" — read captured `allergens_identified`/`allergen_notes`;
  label-render-only ⇒ zero app-screen change); deferred = **beef-labelling regulatory review** (separate job).
  **UI-impact rule answered (also in UI roadmap):** a regulatory field already in the DB = label-only change
  anytime; a NEW staff-entry field = app-screen change → add via existing components, let UI Phase 1 restyle
  (decision #17, no bespoke screens now). One deferred 🔵 follow-up (code-critic 🟡 #1): collapsed-list dead-session
  error renders off-screen at page top on a long list (NOT silent; safety goal met) → fix needs per-row state,
  parked for UI Phase 1. Plan archived to `docs/plans/archive/`. **Next: Pass 2 (Printer port + signed APK).**
- **2026-06-29 (step 5 · docs/repo cleanup — SHIPPED)** — Docs/file-structure cleanup merged (PR #97 squash
  `b1e14e2`, right-sized ANVIL cert `docs/anvil/2026-06-29-docs-structure-cleanup-cert.md` CLEARED). NEW
  `docs/README.md` one-screen index + NEW `docs/reference/{haccp,routing,printing,security}/` evergreen home;
  4 stray root `.md` + 7 loose `docs/` root docs filed; 8 shipped/superseded plans archived; all live-doc
  cross-references repointed; the sealed sixteen-day-roadmap `.html` archived alongside its `.md`. **Pure
  docs + 2 comment-only app edits — zero runtime change** (compiled bundle byte-identical; tsc clean, prod
  build green, CI `smoke` 75/75 live). Kept put (refs would break): `BACKLOG.md`, `adr/**`, `runbooks/`,
  `architecture-review-2026-06-06.md` (cited by 4 ADRs + 2 certs), `ui-current-state.md`, `LABEL_PRINTING_PLAN.md`.
  **Branch sweep (COMPLETE):** all 8 stale local branches deleted — 6 verified-merged (PRs #35/#34/#94/#1/#2/#4)
  + the 2 no-PR ones (`worktree-ui-system-rebuild`, `test/anvil-chrome-matrix`) confirmed fully superseded by
  inspection (worktree docs all in main, equal or older; chrome-matrix spec a pre-route-rename draft of main's
  current version) before deletion. 4 lingering remotes pruned. **Then 18 stale REMOTE branches pruned** (8 PR-merged: f-17/f-24/f-rls-04c·e·f/ui-phase-0a-foundation;
  10 no-PR — the `order-pipeline-*` family + `feature/map-view` + `staging-ccp2-cca-wiring` — each verified an
  ANCESTOR of main = fully contained, nothing unique). **Repo now FULLY clean: only `main`, local AND remote.** **Also recorded:** the
  butcher-facing HACCP handbook is a live DB-backed hexagon (`/haccp/documents` via `HaccpHandbookRepository`),
  separate from the dev docs filed — flagged here as a ports-and-adapters architecture-watch item. **Next: step 6
  — label printing F-PROD-04 (the pivot; pause UI).**
- **2026-06-29 (0b Wave 3 · Overlays + Nav — SHIPPED → PHASE 0 COMPLETE)** — Wave 3 merged (PR #96 squash
  `59a8f13`, docs follow-up `265daa0`): 11 additive overlay+nav components in `components/ui/` (Modal center/sheet ·
  Banner · Spinner · EmptyState · Popover · DropdownMenu · AppHeader · BottomNav · MoreDrawer · DesktopSidebar ·
  NavItem). Purely additive, no new dep, Tier-2 tokens only, no style-leak props; nav bricks presentational
  (logo = caller slot → brand SVGs are a Phase-1 concern, NOT needed for 0b). code-critic SHIP (1🟡 cmd-click ·
  1🔵 safe-area inset, both non-blocking); ANVIL CLEARED (component 81/81 · token guards 9/9 · tsc 0 · lint clean ·
  full unit 2947/2947 · E2E/integration/pgTAP/PITR N/A); CI `smoke` 75/75 first-run. Cert
  `docs/anvil/2026-06-29-ui-phase-0b-wave3-overlays-nav-cert.md`. **→ 0b COMPLETE, Phase 0 done.**
- **2026-06-29 (0b Wave 2 · Display — SHIPPED)** — **Wave 2 SHIPPED** — PR #95 squash `9b91885` (docs follow-up
  `c5186b1`): 11 additive display components + `accent.ts` helper in `components/ui/` (Card, CardHead, KpiTile,
  SectionLabel, PageHeading, ListRow, Table, SegmentedControl, Badge, StatusPill, SyncDot) + dev-only
  `GalleryDisplay` panel. Purely additive (24 new + 2 allowed edits: barrel + gallery wire), no new dep, no Radix
  (all plain semantic HTML), Tier-2 tokens only, no AI refs. **3 API decisions locked at Frame on Hakan's
  "change once → every screen follows" rule** (now roadmap decisions #17/#18 + memory `feedback_design_system_change_once`):
  Table = real semantic `<table>` (NOT grid+`widths`) · SegmentedControl only (no speculative Radix Tabs) ·
  three status components (not one polymorphic Badge); wave rule = no style-leaking props. code-critic SHIP
  no-blockers (2 cosmetic 🔵 only); ANVIL right-sized CLEARED (component 59/59 W2 · lane 138/138 · token guard
  2/2 · tsc/build clean · NO E2E/integration/pgTAP/PITR = N/A presentation-only); CI `smoke` 75/75 fresh; prod
  smoke non-5xx (`/login` 200 · `/orders` 307 auth-redirect · `/kds` 200 · `/api/auth/team` 200 · `/haccp` 200).
  Cert `docs/anvil/2026-06-29-ui-phase-0b-wave2-display-cert.md`; review `docs/reviews/2026-06-29-ui-phase-0b-wave2-display-review.md`.
  **Next: W3 Overlays+Nav (last wave; pull the 7 brand SVGs via DesignSync before it).**
- **2026-06-29 (0b Wave 1 · Forms — SHIPPED)** — At FORGE Frame, 0b was split into **3 themed waves**, each
  its own FORGE→ANVIL→merge PR (Hakan's call, fits the never-batch discipline): W1 Forms · W2 Display ·
  W3 Overlays+Nav (nav last, pull brand SVGs before it). **Wave 1 SHIPPED** — PR #94 squash `ff7a005`:
  a NEW purely-additive `components/ui/` library (11 form components + barrel: Button, IconButton, TextField,
  Textarea, Select, Checkbox, Radio, Toggle, FormField, PinKeypad, Picker) + a dev-only `/dev/ui` gallery.
  ZERO existing files touched, no new dep, no AI refs. code-critic SHIP no-blockers; ANVIL CLEARED
  (component 79/79 · suite 2807/2807 · token guards 13/13 · tsc/lint/build clean · 0 bugs); CI `smoke`
  75/75 GREEN; prod smoke non-5xx. Gallery visually reviewed via local screenshots (it's unreachable on
  deploys — 404 in prod + middleware blocks it → NEW backlog **F-UI-GALLERY-01**: make it preview-reachable).
  Cert `docs/anvil/2026-06-29-ui-phase-0b-wave1-forms-cert.md`. **Next: W2 Display.**
- **2026-06-29 (worktree retired — back to the main dir)** — UI rebuild now runs in the **main working
  directory**, not a separate worktree. Removed the worktree `.claude/worktrees/ui-system-rebuild` and
  force-deleted its squash-merged branch `feat/ui-phase-0a-foundation` (clean, no unmerged work — 0a code is
  already in `main` via PR #93). **Why:** the worktree existed only to isolate UI work from the now-sealed
  16-day re-architecture sprint that was running in parallel on `main`; that sprint is done, so the isolation
  reason is gone. Going forward: cut each section's feature branch straight from `main` here, then PR +
  `smoke` exactly as before — branch protection is unchanged ("work in main" = the main *dir*, NOT committing
  onto the `main` branch). The worktree env-file gotcha below no longer applies (main dir has all env files).
  Several stale local branches remain (`worktree-ui-system-rebuild`, `feat/ui-overhaul-01/02/03`, old
  `f-td-*`/`anvil-*`) — sweep them in step-5 cleanup. **Next: 0b.**
- **2026-06-28 (steps 1–2 DONE — 0a shipped)** — Cert CLEARED: clean-DB `@critical` **75/75** on a prod
  build (run 3; runs 1–2 were worktree test-harness wiring — missing PINs, then missing `SUPABASE_JWT_SECRET`
  — NOT the diff; see ops notes). Merged 0a to `main` via **PR #93** (squash `6a847fa`); `smoke` required
  check green 75/75; merged CLEAN (no admin-override, no F-INFRA-07 caveat — first push). **Production
  redeployed → reskin LIVE** (`mfs-operations.vercel.app/login` serves new orange `#EB6619` + Inter). Local
  `main` synced (removed 2 verified-identical untracked design dupes to unblock the ff-pull). **Next: 0b.**
- **2026-06-28** — Plan created. Terminal-1 took over the UI rebuild from the closed worktree terminal.
  Locked decisions A–D. Step 1 first `@critical` run = 66 fail (worktree lacked `.env.e2e.local` PINs — NOT
  a regression) → copied test env files into worktree → reseed + re-run.
