# MFS Operations App — Backlog

## Allergen Assessment — version history UI missing
The haccp_allergen_assessment table stores each save as a new row (audit trail exists in DB)
but the /haccp/allergens page only ever shows the latest record. There is no UI to view
past versions. Needs a version history list — same pattern as food fraud assessment.
Priority: Medium. SALSA auditor may ask to see previous assessments.

