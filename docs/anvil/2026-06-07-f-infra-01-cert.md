# ANVIL Clearance Certificate — F-INFRA-01

- **Date:** 2026-06-07
- **Branch:** forge/f-infra-01-test-infrastructure
- **HEAD:** cbfa9803ebbe8675ea2fec86207f7aa522496dec
- **PR:** https://github.com/kilichakan2/MFS-Operations/pull/18
- **Stack:** Next.js + TypeScript + Supabase · Diff: infrastructure + scaffolding (no app code)
- **Matrix approved at:** Gate 3, 2026-06-07 by Hakan
- **First PR to use the full pyramid against real infrastructure** — Docker-backed Postgres via Supabase CLI + real Next.js dev server auto-booted by Playwright.

## Layers run

| # | Layer | Command | Result |
|---|---|---|---|
| 1 | Vitest unit (full) | `npm test` | PASS — 1320/1320 |
| 2 | Vitest integration (in-scope) | `npm run test:integration -- withErrors.test.ts observability.test.ts` | PASS — 16/16 |
| 3 | ESLint | `npm run lint` | exit 1 (pre-existing, 55 violations in app/ + components/) · PASS — zero violations in F-INFRA-01 files |
| 4 | TypeScript | `npx tsc --noEmit` | exit 1 (60 errors, identical to main HEAD 0f92122) · PASS — zero new errors in F-INFRA-01 files |
| 5 | Next.js build | `npm run build` | PASS — exit 0, 113/113 static pages |
| 6 | Playwright API smoke | `npm run test:e2e:api` | PASS — 2/2 against local Supabase via auto-booted dev server (2.5s) |
| 7 | Playwright UI smoke | `npm run test:e2e:ui` | PASS — 1/1 in real Chromium (2.0s) |

## Calibrated pass criteria

Layers 3 and 4 exit 1 due to pre-existing project tech debt (tracked as F-TD-01). Baseline verified IDENTICAL to main HEAD `0f92122` — 60 tsc errors, same ESLint violations. F-INFRA-01 introduces ZERO new violations in its own files (`tests/e2e/api/`, `tests/e2e/ui/`, `tests/integration/_assertStack.ts`, `tests/integration/_setup.ts`, `playwright.config.ts`, `vitest.integration.config.ts`). Both F-INFRA-01 greps returned empty output.

## Environment safety verified

- `playwright.config.ts` `webServer.env` block explicitly forwards `NEXT_PUBLIC_SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY` from `.env.test.local` (which points at `localhost:54321`), overriding `.env.local` (which would point at production Supabase).
- `tests/e2e/api/smoke.spec.ts` env guard fires before HTTP test, asserting URL matches `localhost|127.0.0.1` — test 1 of 2 in the API smoke suite is this guard itself.
- Both safeguards verified working in this session: local Supabase health check returned GoTrue v2.189.0 at `localhost:54321`.
- Dev server auto-boot worked cleanly: port 3000 was in use (existing dev server), Playwright correctly reused the server on port 3001 via `reuseExistingServer: true`.

## Layers explicitly out of scope

- Other integration tests (kds/orders-crud/picking-list) — pre-existing Supabase `users.active` schema-cache drift, deferred for future cleanup
- CI / GitHub Actions setup — deferred to future unit

## Advisory items carried forward (not blockers)

From this PR's guard:
- ADVISORY `playwright.config.ts:75` — empty-key fallback `??` for `SUPABASE_SERVICE_ROLE_KEY` should throw rather than silently pass empty string; currently harmless because smoke.spec.ts env guard fires first
- ADVISORY `tests/e2e/api/smoke.spec.ts:50` — GET /api/auth/team passes without auth header; spec validates 200+array shape but is seed-dependent (passes because local Supabase has team rows)
- NOTE `tests/integration/_setup.ts:42` — hard-coded prod project ref pattern in Supabase URL guard; will need updating if project ref changes

Carried from prior PRs:
- ADVISORY `lib/observability/log.ts:67` fallback Caller context
- ADVISORY `lib/observability/log.ts:62` collision merge order
- NOTE `Caller.ts:58` cosmetic test naming
- NOTE KNOWN_ROLES vs Role union — single source of truth
- NOTE 340 console.* migration — future F-TD-02
- NOTE AppError.test.ts dev-mode symmetry
- NOTE F-TD-01 (pre-existing tsc/ESLint debt cleanup)
- NOTE CI workflow setup — future unit

## Closed on this PR

- F-FND-03's small runtime-stack gap — Playwright API smoke now exercises the real Next.js runtime stack end-to-end against local Supabase.

## PITR

Not applicable — no destructive migration in this PR.

## Merge sequence

No migration to push. Standard sequence:

1. Merge PR (Vercel deploys automatically)
2. Confirm Vercel preview build passes

## Verdict

CLEARED for merge. Conductor may present FORGE Gate 4. First full-pyramid clearance certificate — all 7 layers passed their calibrated criteria, zero iterations required.

— anvil-runner (claude-sonnet-4-6, 2026-06-07)
