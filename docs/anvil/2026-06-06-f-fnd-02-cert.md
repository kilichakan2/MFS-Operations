# ANVIL Clearance Certificate — F-FND-02

- **Date:** 2026-06-06
- **Branch:** forge/f-fnd-02-typed-errors
- **HEAD:** 84caf2e3b360fe5f9f0ba9669961a86feaeac9a8
- **PR:** https://github.com/kilichakan2/MFS-Operations/pull/16
- **Stack:** Next.js + TypeScript + Supabase (project) · Diff: real-code TS module + tests
- **Matrix approved at:** Gate 3, 2026-06-06 by Hakan

## Layers run

| # | Layer | Command | Result |
|---|---|---|---|
| 1 | Vitest unit (full) | `npm test` | PASS exit 0 · 1290/1290 pass |
| 2 | Vitest integration (F-FND-02) | `npm run test:integration -- withErrors.test.ts` | PASS exit 0 · 10/10 pass |
| 3 | ESLint | `npm run lint` | WARN exit 1 (pre-existing) · PASS zero violations in F-FND-02 files |
| 4 | TypeScript check | `npx tsc --noEmit` | WARN exit 1 (60 error lines, identical to main) · PASS zero new errors in F-FND-02 files |
| 5 | Next.js build | `npm run build` | PASS exit 0 |

## Calibrated pass criteria

Layers 3 and 4 exit 1 due to pre-existing project tech debt. Both verified IDENTICAL to main HEAD `4d1df22`:

- ESLint: all violations are in `app/admin/`, `app/haccp/`, `app/pricing/`, `components/BottomSheetSelector.tsx` — pre-existing JSX key and no-unescaped-entities nits. Grep for F-FND-02 paths (`lib/errors/`, `tests/(unit|integration)/.*errors`, `withErrors`) returned empty output.
- TypeScript: 60 error lines on the branch; 60 error lines on main. Exact match — F-FND-02 introduces zero new tsc errors. Grep for F-FND-02 paths returned empty output.

This is the Gate-3-approved pass criteria for these layers under the no-net-new-debt rule.

## Baseline re-confirmation (trust-but-verify)

- `git checkout main` → `npx tsc --noEmit 2>&1 | grep -c "error TS"` → **60**
- `git checkout forge/f-fnd-02-typed-errors` → same command → **60**
- Delta: **0** — implementer's claim confirmed.

Note: the matrix stated 107 pre-existing errors; the actual measured count is 60 lines (each multi-line tsc diagnostic collapses to one `error TS` line). The count was re-confirmed independently on both main and the branch — they are identical, which is the material fact. The absolute number difference from the matrix estimate is informational only.

## Layers explicitly out of scope

| Layer | Reason skipped |
|---|---|
| Other integration tests (kds, orders-crud, picking-list) | Require live local Supabase — excluded by Gate 3 decision |
| pgTAP / RLS | No database migration in this PR |
| Deno edge functions | No edge function changes in this PR |
| Playwright E2E | No UI or route changes in this PR |
| Docker compose suite | No compose file / Dockerfile present in repo |
| Supabase preview branch | No schema changes; excluded by Gate 3 decision |
| Vercel preview deploy smoke | Guard confirmed green prior to this run |

## Advisory items carried forward (not blockers)

- BLUE `console.error` in `withErrors` unknown-error path is a stub. Replaced by structured logging + correlation IDs in F-FND-03.
- GREEN Cosmetic test improvement opportunity on `tests/unit/errors/AppError.test.ts`: also assert dev-mode minimum shape. Take or leave.
- BLUE Pre-existing 60 tsc error lines + ESLint nits on main are tracked for a future F-TD-01 (tech debt cleanup) unit.
- BLUE F-INFRA-01 (local Supabase + Playwright API/UI scaffolding) to land just before F-05 (Orders ports).

## Positive scope additions (kept)

- Signature-preservation type-level test at `tests/integration/withErrors.test.ts:150-161` — not in the original plan, defends the strangler-fig migration's one-line-diff promise. Guard recommended keeping; all 10 integration cases pass including this one.

## PITR

Not applicable — no destructive migration in this PR.

## Migration

None.

## Verdict

**CLEARED for merge.** Conductor may present FORGE Gate 4.

— anvil-runner
